# SDD ledger — plan: docs/superpowers/plans/2026-09-03-attribute-definition.md

Controller: orchestrator (no Bash — mechanical script/file-write execution delegated to `git` agent per task; ledger content authored by orchestrator).

## ⏸ RESUME POINT

**Last completed task: Task 20 (Issue rollout wave 1b), fully closed including fix round and re-review sign-off.**
**Last commits on branch `feat/attribute-definition`: `2754a6d0` (refactor: migrate Issue form onto ArtifactForm) → `ca22ea39` (fix: enable severity and tags editability in Issues) → `b6faa590` (fix: support due_date null-clearing in Issue PATCH).**
**Full commit sequence for Task 19: `985ea858` → `5a699ebe` → `e093017d` (CLOSED, rating A). Full commit sequence for Task 20: `2754a6d0` → `ca22ea39` → `b6faa590` (APPROVED_WITH_RECOMMENDATIONS, rating B, 0 blockers).**

**Note (added alongside the Task 23 entry below, banner intentionally NOT rewritten — see next paragraph):** Tasks 21, 23 and 24 ran as 3 concurrent orchestrator instances in this SAME working tree (no git worktree isolation). Each instance appended only its own `## Tasks` entry below and deliberately left this banner untouched to avoid a lost-update race between instances. **Whoever reads this ledger next must reconcile the banner against the actual `## Tasks` section and `git log` before resuming further work** — do not trust the banner's "Next step" line below without cross-checking which of 21/23/24 actually finished and committed by the time you read this.

**Next step when resuming (STALE, see note above — cross-check before trusting): Task 21 (next rollout wave — read `### Task 21:` in the plan to confirm which item type).** No work has been started on Task 22 as of the Task 23 entry below (Task 22's status is unknown to this instance).

**Not pushed to origin.** Recent commits (Task 19, Task 20, and now Task 23's `841aec68`) remain local-only, matching the established local-accumulation pattern on this branch.

**Before resuming any task after 21/23/24, re-read this entire ledger, especially:**
- The corrected deployment-state banner (attribute definitions are LIVE, not dormant).
- All 8 urgent/unassigned gaps below — none have been resolved, all still need a human decision at some point.
- **CORRECTION (found while closing Task 23):** urgent gap #6 and the binding-requirements-tracker line below both hardcoded "Task 23" as the TestCase rollout wave. That is now confirmed WRONG — the actual plan's Task 23 is StakeholderNeed (see `docs/superpowers/plans/2026-09-03-attribute-definition.md` line 9662, `### Task 23: Rollout wave 2c — StakeholderNeed`), not TestCase. The `steps`/`steps_data` HARD BINDING in gap #6 still applies, but to whichever task number the plan actually assigns to the TestCase wave — re-read the plan directly to find the correct task number before that wave starts, do not trust the old "Task 23" label anywhere in this ledger.
- The Task 19/20/23 entries' pattern: EVERY rollout wave so far (Risk, Issue, StakeholderNeed) found real bugs on first contact with a live bootstrapped definition — usually more than one, and usually only fully surfaced by an independent review pass, not by the implementer's own live-verification alone. Task 23 additionally showed that a REVIEW-DRIVEN FIX ROUND ITSELF can introduce a new regression that only a fully independent, fresh-eyes re-review catches — see the Task 23 entry below. Expect the same risk class for ArchitectureElement/TestCase/GlossaryTerm/Requirement waves.
- The hard binding on the TestCase wave (gap #6, corrected task-number pending re-verification per the correction note above) - this is the most severe known landmine still ahead.

## Preflight Scan

Plan is phase-sequential and file-additive. Tasks 15-18 (shared frontend library) complete. Task 19 (Risk), Task 20 (Issue) and Task 23 (StakeholderNeed) rollout waves complete. Task 21 (ADR) implementation complete but commit status unknown to this instance (see banner note). Task 24 (ArchitectureElement) was running concurrently with Task 23; status unknown to this instance. Remaining rollout waves and Task 27 (removes the legacy frontend config UI) status to be re-checked on next resume.

## ⚠ CORRECTED DEPLOYMENT-STATE UNDERSTANDING (Task 13 finding)

**Attribute definitions are NOT dormant-until-manually-bootstrapped. They have been LIVE in every deployment since Task 8.** This has already caught 7+ CRITICAL, live bugs across Tasks 11/13/14/17/19, plus 2 more (severity/tags hidden, due_date-clear broken) in Task 20. **Every remaining task must be checked against "what does this break for an already-effectively-bootstrapped workspace", including deliberately probing what an ADMIN CAN ADD (widening), not just what they can hide (narrowing).**

## ⚠ UNASSIGNED / URGENT PLAN GAPS — flag to user, human decision needed (8 total, none resolved)

1. **`validate_artifact_fields` is enforced on the 9 REST ViewSets (Task 11) and NOT enforced on MCP artifact-write tools or the CSV bulk importer.** No task in this 27-task plan ever closes this gap. Live since Task 11.
2. **`describe_attribute_schema` still returns a static, hardcoded field list, not the real customized definition.** Needs a task assignment.
3. **The legacy frontend custom-fields UI has been calling deleted REST endpoints since Task 9, visibly broken in 8 live forms, NOT fixed until Task 27 (~7 tasks away).** Needs a human decision.
4. **`ChangeRequest` is a real, ViewSet-backed, workflow-managed artifact type with NO attribute definition at all** - same unassigned-gap class as `Goal`/`Icd`.
5. **The backend never enforces which properties a meta-only PUT on a locked/core attribute may change beyond kind/locked** - `CORE_EDITABLE_META_PROPERTIES` (`schema.py:58-63`) is defined and exported but never referenced by `validate_meta_only_change`. Live-proven: an admin can freely narrow/widen a widget's `fields` list AND swap its `widget_key` to an incompatible one, and the backend accepts it. Task 18 added client-side guards for the resulting frontend degradation, but the underlying backend contract remains unenforced.
6. **`TestCase.steps` has a backend vocabulary split that will cause silent, total data loss the moment the TestCase rollout wave ships.** `bootstrap_attribute_definitions.py` emits a widget attribute named `steps` bound to `fields: ["steps_data"]`, but the real model column AND the REST serializer are both called `steps` - `steps_data` doesn't exist anywhere in the backend. Live-proven: existing steps become invisible, edits are silently discarded, no error shown. **HARD BINDING for the TestCase rollout wave — CORRECTED: this is NOT Task 23 (confirmed to be StakeholderNeed, see the RESUME POINT correction note above). Re-check the plan directly for the actual TestCase task number before that wave starts.**
7. **`custom_fields` (extended attributes) are completely unsupported by `RiskSerializer`/`AdrSerializer`/`IssueSerializer` at every layer** - no `CustomFieldsSerializerMixin`, no `custom_fields` param on the underlying service update methods. Live-proven with Requirement as a working control: a PATCH with `custom_fields` on Risk/Adr/Issue returns 200 but the value is silently discarded (never persisted). Not live-reachable today (freshly bootstrapped definitions have zero extended attributes), but the admin path to create one is fully open and unguarded on these 3 item types specifically - one admin action away, no warning. **Reconfirmed unchanged in Task 20** (Issue): `IssueArtifactForm` sends `custom_fields: {}` on every save, silently discarded, same failure class as Task 19 found for Risk/Adr. **StakeholderNeed (Task 23) is a WORKING CONTROL, unlike Risk/Adr/Issue**: `StakeholderNeedSerializer` genuinely carries `CustomFieldsSerializerMixin` and `update_need()` persists `custom_fields` correctly — live-proven via PATCH/GET round-trip. No task in this plan owns fixing the Risk/Adr/Issue gap itself.
8. **NEW (Task 20 finding): `Issue.assignee_id`/`assign_issue()` is a complete, REQ-backed feature (REQ-L3-ISSUE-008, dedicated audit trail via `assignee_changed_date`) with ZERO REST or MCP route ever calling it.** `update_issue()`'s own docstring says assignment deliberately does not go through the generic update path. Task 20 correctly did NOT wire this up (out of scope) and instead safely excluded `assignee_id`/`assignee_changed_date` from attribute introspection so they can't be silently dropped via the generic PATCH path. A human decision is needed on whether/when a dedicated `/issues/{id}/assign/` endpoint should ship - same unassigned-gap class as #4.

## Binding requirements tracker (cross-task, apply at the named future task)

- **Unbound, needs a task assignment when read:** `Goal`, `Icd`, and `ChangeRequest` are item types with no enforcement. (Task 11 review I-4, Task 16 review, urgent gap #4)
- **HARD BINDING for the TestCase rollout wave (task number CORRECTED — NOT Task 23, see urgent gap #6 and the RESUME POINT correction note):** see urgent gap #6 - the `steps`/`steps_data` name split must be mapped before that wave ships.
- **HARD BINDING for EVERY remaining rollout wave, proven the hard way on Tasks 19, 20 and 23:** before considering a wave done, diff `set(real GET payload keys)` against `_PROTECTED_PATCH_FIELDS` (`backend/rest_api/mixins/workflow_transitions.py:43-64`) using a FULL, realistic payload (not a hand-built minimal one) run through the actual component's save pipeline. Task 19 shipped with a 100%-failure bug (`uid` leaking into every Risk PATCH) that a minimal-fixture-based "live verification" completely missed, and was only caught by an independent reviewer rebuilding the real emitted patch. The `uid` root cause is fixed at the shared introspection point for ALL item types (commit `e093017d`), so `uid` itself should not recur - but the METHOD (verify against a full real payload, not a minimal one) must be applied fresh to each new wave. **Task 20 adds a second lesson: a wave's own live-verification can pass cleanly (no protected-field leak) while still shipping an EDIT-CAPABILITY regression (fields silently dropped from the introspected definition, or a null-clear silently ignored) that a leak-focused check alone won't catch.** Also always independently verify a "field X was excluded on purpose" claim in the bootstrap command against the actual model/serializer for the item type in front of you - a globally-scoped exclusion (like `severity`) can be correct for one item type (Risk, derived) and wrong for another (Issue, plain writable field) sharing the same column name. **Task 23 adds a third lesson: a fix round's own new code can reintroduce the exact bug class it was fixing (there: fixing a `changeReason`-dirty gap while shipping a new `custom_fields`-dirty gap; separately, fixing a `custom_fields` visibility gap by adding a merge that silently broke key removal/rename because the backend REPLACES the field wholesale rather than patching it).** After any review-driven fix round, a subsequent review must treat the WHOLE diff as suspect again, not just re-check the specific findings that triggered the round — do a full fresh-eyes pass, and explicitly test the interaction between the round's own multiple fixes, not just each fix in isolation.
- **Binding for any rollout wave touching widgets:** a wave must not feed a live-refetching object into `ArtifactForm`'s `initialValues` while the form is dirty - by design, a poll returning changed data legitimately overwrites unsaved edits.
- **Recommendation for rollout waves generally:** `ArtifactForm` does not enforce `required` client-side (only `aria-required`) - relies entirely on the backend's per-field 400 response, now correctly routed through the UI.
- **`frontend/src/test/protected-patch-fields.ts`** (new in Task 19) is a reusable helper mirroring the backend's `_PROTECTED_PATCH_FIELDS` set - future rollout waves should use it for their own regression tests rather than re-deriving the list. It is hand-synced with no drift guard (acceptable per Task 19 sign-off review, but worth a codegen/assert step if a field is ever added to the backend set without a corresponding frontend update).
- **Binding for any rollout wave with a free-form `custom_fields` UI need (Task 23 finding):** if the live attribute definition has zero `kind:"extended"` attributes for that item type (true for StakeholderNeed as of Task 23), a sibling `CustomFieldsEditor` component feeding an OVERWRITE (not merge) into the PATCH body is the correct, safe pattern — a merge against `need.custom_fields`/equivalent silently breaks key removal and rename, because these services REPLACE the whole `custom_fields` map server-side rather than patching it. Re-evaluate only if/when a real `kind:"extended"` attribute ships for that item type (see urgent gap #7's still-open Risk/Adr/Issue half). Any such sibling editor's dirty-state must also be wired into the parent's combined dirty-check AND explicitly cleared (`markXClean(...)`) after a successful save, or the unsaved-changes guard either false-fires forever or silently stops firing.
- **Cleanup backlog, non-blocking:**
  - `GlobalAttributeDefinitionStore._propagate`/`list_derived_workspace_ids` cache invalidation done by 2 external callers individually (safe today).
  - Task 8's migration (0003) still uses `print()` instead of `logging.getLogger(__name__)`.
  - Task 9's migration (0080) leaves the now-orphaned `pl_custom_field_value` table in place.
  - Task 9's value-fold applies only 2 of `validate_custom_fields`'s ~7 rules - bounded, user-recoverable.
  - `e2e/tests/hermes-bugfix-campaign.spec.ts:325`'s test title says "returns 200" while its assertion checks 404.
  - `rest_api/tests/conftest.py` has 2 near-duplicate tenant/workspace/client fixture families.
  - `bootstrap_attribute_definitions.py::_append_missing` leaves an unresolved `F()` expression with no `refresh_from_db` (safe today).
  - No `@extend_schema` on the 4 attribute-definition REST routes.
  - `_workspace_preset`'s `except (..., ValueError)` is wider than needed.
  - `_resolve_workspace_id` fails OPEN on a bare `except Exception` (deliberate, deserves logging).
  - `EXCLUDED_MODEL_FIELDS`'s `suspect`/`current_revision` are redundant with the `has_default()` fix.
  - Task 11's `mandatory_fields`-to-Requirement-only fix is a deliberate DEVIATION from the plan's own snippet - don't "fix" it back.
  - `.meta-config/project.yaml` still says "171 Tools" - the generator source for `CLAUDE.md`.
  - `interview_protocol.py::_validate_protocol_config` (Task 13) is unreachable dead code, kept deliberately.
  - `interview_protocol.py`'s "no such workspace" detection (Task 13) uses a message-text substring match.
  - `resolve_export_fields`'s `except AttributeDefinitionNotFound` (Task 14) conflates 3 conditions - documented, safe today.
  - `get_bundle`'s error precedence changed (Task 14) - documented, judged correct.
  - THREE leftover `git stash` entries remain (Task 15, Task 18 fix round, Task 19 fix round incidents) - all harmless/superseded, guard blocks `stash drop` even with approval; user can drop manually outside the agent framework (`git stash list` to see all, drop each after visual confirmation of content).
  - Frontend test-run variance host vs. container across Tasks 15-19 - in-container/bind-mounted vitest is the reliable baseline.
  - `ReferencePicker.tsx` fires one API request per keystroke (harmless, no debounce).
  - `frontend/src/components/DiagramView/DiagramCreateForm.tsx:103` - pre-existing (not SDD-caused) TS build error from the React 19 bump; `npm run build` fails on `main` too.
  - No reference-target-type field exists in `AttributeSpec` for `type:"reference"` attributes; no cross-type "list artifacts of type X in workspace" endpoint exists anywhere in the frontend.
  - `MarkdownTabGroup.tsx`'s active-tab index doesn't re-sync if `attribute.fields` changes after mount (Task 17 review, M-1).
  - `RiskMatrixRpz.tsx`'s error aggregation collides on React key when 2 fields share the same error message (Task 17 review, M-2).
  - `RiskMatrixRpz.tsx`'s `<select>` has no empty option (Task 17 review, M-3).
  - `computeRpn` misreports for a string-typed `detection` value (Task 17 review, M-4).
  - `ArtifactForm.tsx`'s `key` prop is nested inside a spread `shared` object, triggering a React deprecation warning on every field render (Task 18 review, M-1).
  - `ArtifactForm.tsx`'s section auto-expand doesn't force-open a section containing an unsupported-widget alert (Task 18 review, M-2).
  - `ArtifactForm.tsx`'s `expanded` (section open/closed) state isn't reset when `artifactId` changes (Task 18 review, M-5).
  - The old `RiskForm.tsx`'s editable `severity` `<select>` was removed in the Task 19 migration - this is CORRECT (severity is derived from `risk_score`, not user-settable), not a regression, but worth knowing if anyone notices the field is "missing".
  - **Task 20:** no binding regression test locks in the `severity`-editable-on-Issue / `tags`-via-`tag_list`-widget fix at the bootstrap-command level (`test_bootstrap_command.py`) or asserts `severity` absent-on-Risk - the fix is correct today but unguarded against silent revert (review N-1).
  - **Task 20:** `Issue.due_date` set through the UI's `<input type="date">` truncates any sub-day time component to midnight on the next save - only reachable now because Task 20 is the first task to expose `due_date` in the REST/UI surface at all; day-granularity round-trips correctly (review N-2).
  - **Task 20:** naming convention is inverted vs. `TestCase`'s `steps`→`steps_data` widget aliasing - Issue's `tag_list` widget deliberately keeps the raw `tags` field name in the definition (load-bearing, not an oversight) but the asymmetry deserves one explanatory comment (review N-3).
  - **Task 20:** `IssueSerializer.due_date`'s `default=None` is redundant (partial-update `SkipField` already handles the omitted case) - harmless, one token shorter without it (review N-4).
  - **Task 20:** `IssueArtifactFormProps.onDirtyChange` is declared/forwarded but `IssueEditors.tsx` never passes it - dead prop until a caller wants it (review N-5).
  - **Process note (Task 20):** the `git` agent used `git stash push -- <excluded files>` to keep 2 unrelated pre-existing modifications out of the Task 20 commits, then initially left the stash unpopped (working tree briefly showed those 2 files as clean/reverted-to-HEAD instead of their original locally-modified state). Caught and corrected by an orchestrator follow-up check before this ledger entry was written; no data was lost (stash still had the content). **Future tasks: verify any stash used for commit-exclusion purposes is popped back in the SAME delegation, don't just trust the git agent's self-report.**
  - **Task 23:** 2 regression tests from the deleted `need-form-change-reason.test.tsx` were not migrated — `"hides the change_reason field for the Minimal preset"` (genuinely redundant, dropped intentionally) and `"uses the need-specific change_reason placeholder, not the requirement one (#418)"` (a named guard for a previously-shipped bug; behavior is still correct today but now unguarded against silent revert — worth a one-line re-add in a future pass, not urgent).
  - **Task 23:** `description` lost its `MarkdownPreview` editor in the migration (now a plain textarea, matching the live attribute definition's `type: text`) — same class of capability drop as ADR's Task-21 Supersede-Flow risk, but here NOT preserved (no `markdown_tab_group` binding exists for a single-field case). Risk/Issue never had a markdown editor for comparison; ADR (Task 21) explicitly preserved its 3-field markdown group one wave earlier. No explicit human ruling was made on whether StakeholderNeed's `description` should also get a markdown widget — flagging for a decision, not a defect.
  - **Task 23:** `NeedsEditors.tsx` does not render a standalone `ArtifactId`/`StatusBadge`/`VersionBadge` identity header — consistent with the already-shipped Risk/Issue editors (verified against their real current source), but this is now 3 consecutive waves without it despite the plan text and UI concept ch. 12.4 implying one. No E2E impact (no such selectors exist). Needs a human ruling before more waves accumulate the same drift.
  - **Task 23:** a workspace with no bootstrapped `WorkflowDefinition` makes `DELETE /needs/<id>/` return an unhandled `500` (`WorkflowDefinitionError` falls into a bare `except Exception` at `rest_api/views.py:639`) instead of a 4xx — pre-existing, not caused by this task, only surfaced by this wave's live DELETE probing.
  - **Task 23:** a freshly created tenant with no `GlobalAttributeDefinition` rows makes `GET /workspaces/<id>/attribute-definitions/<Type>/` 404, so every migrated rollout-wave form (all of Risk/Issue/Adr/StakeholderNeed so far) renders its load-error state until Task 8's migration/bootstrap has actually run for that tenant — confirms the ledger's "Task 8 is the real bootstrap entrypoint" note is load-bearing, not just historical.
  - **Task 23:** `ui-ratchet.test.ts`'s `STYLE_BRACE_BASELINE` uses exact equality (`toBe`, not `<=`) on a file 3 concurrent rollout waves (21/23/24) all edit in the same shared working tree — the value moved twice during Task 23's own review cycles purely from unrelated concurrent edits landing/being reverted. Not a bug in any single wave, but the ratchet's exact-equality shape is the wrong tool for a contended shared file; consider a `<=` ratchet or a per-wave delta assertion instead, next time multiple waves run concurrently.

## Tasks

Task 1: complete (commit 0eecfe3c..728e890d, review APPROVED_WITH_RECOMMENDATIONS, 2 parked)
Task 2: complete (commit 728e890d..a688a84b, review APPROVED_WITH_RECOMMENDATIONS) — privilege-escalation gap closed at Task 11.
Task 3: complete (commit dd3e0f10, review APPROVED_WITH_RECOMMENDATIONS, no fix round) — guard discovery: ALL commits route through `git` agent from here on (user ruling).
Task 4: complete (commit 52c98881, fix round c0c508e7, review CLOSED, rating A) — global store + propagation.
Task 5: complete (commit 920b598c, review APPROVED_WITH_RECOMMENDATIONS, rating A−, no fix round) — workspace store.
Task 6: complete (commit 787a9eff, review APPROVED_WITH_RECOMMENDATIONS, rating A−, no fix round) — bootstrap command; 2 real brief bugs found+fixed live.
Task 7: complete (commit 6a786387, fix round 01d99c67, review CLOSED, no further round) — `AttributeDefinitionService` facade.
Task 8: complete (commit db7568e7, fix round a1253207, review APPROVED (2/2), rating A) — legacy data migration; 4 CRITICAL bugs proven live and fixed. **This migration is the actual, unconditional bootstrap entrypoint for the whole feature.**
Task 9: complete (commit e968c04a, fix round 4d4c4a1d, review CLOSED (2/2), rating B) — retired legacy field-config mechanisms; 1 CRITICAL data-loss bug found+fixed. (Frontend side unfixed until Task 27, see urgent gap #3.)
Task 10: complete (commit f33b816c, security fix round ea3bb7a6, review APPROVED_WITH_RECOMMENDATIONS, rating B) — REST endpoints; reviewer found AND personally fixed a CRITICAL cross-tenant data disclosure.
Task 11: complete (commit 563b2ccb, fix round 7835fc23, review APPROVED (2/2), rating A) — wired validation live into 9 ViewSets. 1 CRITICAL live-production-breaking bug found+fixed.
Task 12: complete (commit bf0fbd54, review APPROVED_WITH_RECOMMENDATIONS, rating A−, no fix round) — `attribute_definition.{list,get,update,reset}` MCP tools (170→174 tools).
Task 13: complete (commit 2f893758, fix round 9aa36d74, second fix round 1c928e43, review CLOSED, rating B) — derived interview protocol; discovered the corrected deployment-state understanding, plus 2 rounds of CRITICAL data-loss bugs.
Task 14: complete (commit b48dccdf, fix round c1e284e9, review APPROVED, no further round) — resolved export fields; a 3rd CRITICAL widening-direction bug found by review.
Task 15: complete (commit 6fe03556, review APPROVED_WITH_RECOMMENDATIONS, rating A−, no fix round) — frontend API wrapper; zero brief deviations. Surfaced the 8-form legacy custom-fields frontend breakage (urgent gap #3).
Task 16: complete (commit 972a98fb, review APPROVED_WITH_RECOMMENDATIONS, rating B+, no fix round) — ArtifactForm field component library (10 field types); disproved the controller's own suggested item-type list; found+fixed a UI-ratchet collision.
Task 17: complete (commit 4ffe5212, fix round a1c5a5ec, review CLOSED, no further round) — ArtifactForm widget registry (3 widgets); 1 CRITICAL data-shape bug found+fixed; surfaced the unenforced-backend-contract gap (urgent gap #5).

**FRONTEND SHARED LIBRARY MILESTONE (Tasks 15-18) COMPLETE.**

Task 18: complete (commit 40da36fb, fix round ab25cf0b, review APPROVED_WITH_RECOMMENDATIONS, rating B+, no further round)
- `ArtifactForm.tsx`, the top-level definition-driven renderer integrating Tasks 15-17. Closed all 3 explicit bindings assigned to this task. 7 additional brief deviations found+fixed, most notably an `initialValues` reset bug that would have silently discarded in-progress typing across ALL upcoming rollout waves.
- Reviewer's adversarial end-to-end test against a REAL bootstrapped Risk definition found 2 more Important findings (self-owning widget vanishing; the initialValues fix itself being key-order-sensitive). Fix round closed both, re-anchoring the reset on `artifactId` via the codebase's existing `useEntityReset` hook.
- Surfaced urgent gap #6 (TestCase steps/steps_data split).

Task 19: complete (commits 985ea858, 5a699ebe, fix round e093017d, sign-off review CLOSED, rating A) — **ROLLOUT WAVE 1 OF 7 (Risk)**
- First rollout wave: migrated `RiskForm` onto `ArtifactForm` (`RiskArtifactForm.tsx`), the first real production usage of the entire Tasks 15-18 shared library. Implementer proactively found+fixed 2 real backend bugs before review even started, root-caused at the shared `bootstrap_attribute_definitions.py` introspection point (so the fixes apply to ALL item types, not just Risk): (1) `owner_user` attribute name didn't match `RiskSerializer`'s real field (`owner_user_id`) - would have silently discarded every owner assignment; (2) `created_by_name` was exposed as visible/editable but supported by no serializer for Risk, Adr, or Issue - same silent-discard class, fixed universally in one place.
- Reviewer found 1 CRITICAL merge-blocking bug the implementer's own "live verification" missed because it used a hand-built minimal PATCH instead of the component's real emitted payload: `uid` leaked into every Risk save's PATCH body (bootstrap introspection marked it `editable=True` despite it being in `_PROTECTED_PATCH_FIELDS`), causing **100% of Risk saves to fail with a 400**. Also confirmed and elevated a previously-suspected gap to CRITICAL-verified status (urgent gap #7): `custom_fields` support is entirely absent from Risk/Adr/Issue's serializers at every layer, live-proven with Requirement as a working control - not live-reachable by default, but Task 19's migration makes the failure mode WORSE (silent success instead of a visibly broken legacy component).
- Ruling: fix round required immediately (100%-failure merge blocker, and the root cause is a template bug the next 6 waves would otherwise copy verbatim). Fix round (`e093017d`) fixed `uid` at both the shared backend introspection point (confirmed to also affect and fix Adr/Issue) and defensively on the frontend, added a generic cross-item-type structural regression test ("no introspected attribute is both protected and editable"), and a reusable frontend test helper for future waves. Proven via revert-and-restore at both layers plus a full live end-to-end round-trip (real GET → real component pipeline → real PATCH → 200) AND a negative control (reproducing the original 400 exactly by re-adding `uid`).
- Sign-off review (final pass before this session paused): CLOSED, rating A. Independently re-verified the fix applies correctly to all 7 affected item types, re-ran revert-probes on both layers, confirmed no new required-but-uneditable trap and no missing entry in the frontend's defensive exclude list for Risk specifically, full regression green.
- Urgent gap #7 added to the banner - `custom_fields` unsupported on Risk/Adr/Issue, needs a task assignment/human decision, same unresolved-gap class as #1-#6.

Task 20: complete (commits 2754a6d0, ca22ea39, b6faa590, fix round bundled into ca22ea39/b6faa590, re-review APPROVED_WITH_RECOMMENDATIONS, rating B) — **ROLLOUT WAVE 2 OF 7 (Issue)**
- Second rollout wave: migrated `IssueForm` onto `ArtifactForm` (`IssueArtifactForm.tsx`), same pattern as Task 19. Implementer found+fixed 2 real backend bugs live before the first review: (1) `IssueSerializer` had no `due_date` field at all despite `IssueService.create_issue`/`update_issue` already fully supporting it - a pure pre-existing REST gap, closed by adding the serializer field + view passthrough; (2) `assignee_id`/`assignee_changed_date` were live-introspected as editable despite `update_issue()` deliberately routing assignment through a separate `assign_issue()` method (its own audit trail, REQ-L3-ISSUE-008) that no REST/MCP route ever calls - correctly excluded from introspection rather than shipped as a silent-drop trap (same fix class as Task 19's `created_by_name`). Confirmed the `_PROTECTED_PATCH_FIELDS` diff clean (0 leaks) via a real GET → real form pipeline → real PATCH round-trip, and confirmed urgent gap #7 (`custom_fields`) unchanged for Issue as expected (out of scope for this task).
- First-round reviewer independently found 1 REQUIRED regression the implementer's live-verification missed (a leak-focused check, not an edit-capability check): `severity` and `tags` became **uneditable everywhere in the UI** after the migration. Root cause: `severity` sat in the bootstrap command's GLOBAL `EXCLUDED_MODEL_FIELDS` set (correct for `Risk.severity`, which is genuinely derived from `risk_score`; wrong for `Issue.severity`, an ordinary user-editable choice field sharing the same column name), and `tags` (a `JSONField`) had no widget mapping at all so it was silently dropped from introspection. Also found 1 should-fix bug: `due_date` could be set via PATCH but never cleared - `IssueService.update_issue` gated `if due_date is not None`, so an explicit `null` from the UI's date-clear control was silently ignored (live-proven: PATCH 200, value unchanged).
- Ruling: fix round required (1 blocker: silent UI edit-capability loss on 2 fields; 1 should-fix bundled in). Fix round split `EXCLUDED_MODEL_FIELDS` into a new per-item-type `PER_ITEM_TYPE_EXCLUDED_FIELDS` override (Risk-only for `severity`) plus a new reusable `tag_list`/`tag_input` widget (wraps the existing `tag-input.tsx` component, no reimplementation) for `severity`+`tags`; fixed `due_date` clearing with an `_UNSET` sentinel default in `update_issue` (reusing an existing in-repo pattern from `RequirementViewSet.partial_update`) so the view can distinguish "field omitted" from "field explicitly cleared to null"; added binding regression tests for both (frontend `IssueArtifactForm.test.tsx` F-1 block, new backend `test_issue_due_date_patch.py` covering set/omit-unchanged/explicit-null-clear).
- Re-review (iteration 2/2): APPROVED_WITH_RECOMMENDATIONS, rating B, 0 blockers. Independently re-verified both fixes live (Issue now shows `severity`+`tag_list` editable, Risk still excludes `severity`, no regression on TestCase's `steps` widget), re-checked `_PROTECTED_PATCH_FIELDS` clean via a fresh real round-trip, re-verified the `_UNSET` sentinel mechanism at the DRF `validated_data` layer (not just by reading), and checked all call sites of `update_issue` (only the one production caller, no PUT route exists). 236 backend tests green (isolated DB - a shared-DB test-container collision was reproduced and confirmed environmental, not code), 31/31 targeted frontend tests green, full frontend suite 1536/1537 (1 unrelated pre-existing failure). Left 5 non-blocking notes for the cleanup backlog (missing binding test for the F-1 fix itself, a now-reachable date/time-truncation ceiling on `due_date`, a naming-convention asymmetry vs. TestCase's widget aliasing, a redundant serializer default, a dead prop) - see cleanup backlog above.
- New urgent gap #8 added to the banner - `Issue.assignee_id`/`assign_issue()` has no REST/MCP route, same unassigned-gap class as `ChangeRequest` (#4), needs a human decision on a future `/assign/` endpoint.
- Process incident: the `git` agent used a stash to keep 2 unrelated pre-existing file modifications out of the commits and initially left it unpopped; caught and corrected before this entry was written (see cleanup backlog "Process note").

Task 21: implementation complete, NOT YET COMMITTED as of this Task 23 entry (developer-role dispatch; commit deferred to the `git` agent per this task's explicit constraints) — **ROLLOUT WAVE 2a OF 7 (ADR)**
- Third rollout wave: migrated `AdrForm` onto `ArtifactForm` (`AdrArtifactForm.tsx`), same pattern as Tasks 19/20. First wave whose special case needed a widget: the three stacked `MarkdownPreview` editors (description/context/consequences) became one `markdown_tab_group` widget (`decision_record`, already present in `bootstrap_attribute_definitions.py`'s `WIDGET_ATTRIBUTES["Adr"]` since before this task — no backend change was needed for it). `Adr.decision` (a fourth markdown column, backend #373) was never bound to the deleted form's editors either, and stays an ordinary textarea attribute, unchanged from before.
- DEVIATION from the plan brief, found before implementation by reading the deleted `AdrForm.tsx` in full rather than only the plan's Step-3 snippet: the real ADR editor also carried a whole ADR-specific capability, the **Supersede-Flow** (REQ-L3-ADR-005, UI-32/Systemaudit 2026-08-27 AP-5 — "Supersede durch..." button, candidate picker, reason textarea, "Abgelöst durch: ..." resolution via the `decides` TraceLink), plus an `isExtendedPreset`/`change_reason` capture block. `ArtifactForm` has no concept of either. The plan's literal Step-3/4 snippets would have silently deleted the Supersede-Flow entirely (a real, REQ-backed, workflow-gated capability, not cosmetic) — same failure class as Task 20's severity/tags edit-capability regression, just caught before merge instead of by a reviewer after. Fix: extracted the Supersede-Flow verbatim (same state machine, same styles, same `data-testid`s, same regression tests) into a new standalone sibling component, `AdrSupersedePanel.tsx`, rendered next to `AdrArtifactForm` by `AdrEditors.tsx` — not inside it, since `ArtifactForm`'s render loop has no hook for an artifact-specific lifecycle action. The `change_reason`/`isExtendedPreset` block was NOT carried over — confirmed via `git show` on Tasks 19/20 that `RiskForm`/`IssueForm` had the identical block and neither `RiskArtifactForm` nor `IssueArtifactForm` carries it either; the plan (line ~10183) explicitly assigns adding `change_reason` support to `ArtifactForm` itself to the later Requirement task. **CORRECTION from the Task 23 entry below: this "later Requirement task will add it centrally" assumption did NOT hold — Task 23 (StakeholderNeed) needed its OWN per-item-type `change_reason` handling because `StakeholderNeedService.update_need`/`delete_need` already enforce it today via `PresetPolicyService.is_change_reason_required()` on Extended-preset workspaces, and `ArtifactForm` still has no shared concept of it. Whichever task actually implements the shared version needs to account for at least Adr and StakeholderNeed already having (or needing) their own local version by then.**
- `e2e/` grep for `adr-form`/`adr-description`/`adr-context`/`adr-consequences`/etc.: zero matches — no E2E selector rewrites needed (Step 5 was a no-op for this wave, unlike the plan's generic guidance). The Supersede-Flow's own `data-testid`s (`adr-supersede-btn`, `adr-supersede-target-select`, etc.) are unchanged since the component that owns them kept its exact markup, just moved files.
- Live-verified via a real GET → real form pipeline → real PATCH round-trip against the actual dev backend (not a hand-built minimal payload; created a real ADR via `POST /api/v1/adrs/` in the Demo Workspace — extended preset — under an admin JWT minted via `PasswordAuthenticationService.issue_token`, `GET` it, ran the exact `adrToFormValues` → edit → `formValuesToAdrPatch` logic in Python mirroring the TS, `PATCH`ed): **200 OK**, `decision` field changed correctly, `version` incremented 1→2, PATCH body was `{title, description, context, decision, consequences, custom_fields}` — **0 of the 11 `_PROTECTED_PATCH_FIELDS` present** (verified: `id`, `workspace_id`, `uid`, `status`, `version`, `created_at`, `updated_at` all correctly absent). Negative control: re-adding `uid` to the same PATCH body reproduced the expected `400 VALIDATION_ERROR` ("'uid' is read-only and cannot be set via PATCH."), confirming the shared Task-19 fix (`READ_ONLY_MODEL_FIELDS` in `bootstrap_attribute_definitions.py`) already covers Adr with no wave-specific backend change required this time. Test ADR deleted after verification (`DELETE .../adrs/<id>/` → 204).
- Edit-capability check (Task 20's standing lesson — a clean protected-field diff alone is not sufficient): compared the introspected/editable attribute set against the deleted `AdrForm`'s own field list. `title`/`description`/`context`/`consequences`/`decision`/`status` (via `WorkflowStatusEditor`, now rendered generically by `ArtifactForm` for `editable: "workflow"` attributes) all remain editable; no field became silently uneditable. No `PER_ITEM_TYPE_EXCLUDED_FIELDS` or widget-mapping gap analogous to Task 20's `severity`/`tags` was found for Adr — `bootstrap_attribute_definitions.py` required no changes for this wave.
- Urgent gap #7 (`custom_fields` unsupported by `AdrSerializer`) reconfirmed UNCHANGED and CONFIRMED STILL OPEN for Adr specifically: read `AdrSerializer` (backend/rest_api/serializers.py:1320-1359) directly — no `CustomFieldsSerializerMixin`, no `custom_fields` field declared. Live-proven again in the same round-trip above: `formValuesToAdrPatch` sends `custom_fields: {}` on every save (same as Risk/Issue), backend returns 200 and silently drops it (the response body has no `custom_fields` key at all). No wave-specific new risk introduced — `AdrArtifactForm` widens nothing here that Risk/Issue didn't already widen; still needs the same standing human decision as gap #7's Task 19/20 entries.
- `ui-ratchet.test.ts`'s `STYLE_BRACE_BASELINE` re-measured and lowered: 979 → 968 (-11) at the time of this task's own work. The deleted `AdrForm.tsx` carried both the hand-written layout (now `ArtifactForm`-owned, contributes zero `style={{`) and the Supersede-Flow (moved unchanged into `AdrSupersedePanel.tsx`, same inline styles as before, just in a smaller file) — net delta is the layout portion only, matching the Task 19/20 pattern of the deleted hand-written form's non-widget chrome disappearing. **Note: this value moved further during Task 23's own review cycles due to concurrent editing of the same shared file — see the Task 23 entry's cleanup-backlog note on the ratchet's exact-equality shape.**
- Files: created `frontend/src/components/AdrEditors/AdrArtifactForm.tsx`, `frontend/src/components/AdrEditors/AdrSupersedePanel.tsx` (new, DEVIATION above), `frontend/src/test/AdrArtifactForm.test.tsx`, `frontend/src/components/AdrEditors/AdrSupersedePanel.test.tsx` (renamed+retargeted from the deleted `AdrForm.test.tsx`, same 4 tests verbatim); modified `frontend/src/components/AdrEditors/AdrEditors.tsx` (import + render site + null-guard moved to call site, same pattern as RiskEditors/IssueEditors), `frontend/src/test/ui-ratchet.test.ts` (baseline); deleted `frontend/src/components/AdrEditors/AdrForm.tsx`, `frontend/src/components/AdrEditors/AdrForm.test.tsx`.
- Test results: `AdrArtifactForm.test.tsx` 7/7 green, `AdrSupersedePanel.test.tsx` 4/4 green, `AdrEditors.test.tsx` 11/11 green (pre-existing, unmodified — passed unchanged against the new component wiring), `ui-ratchet.test.ts` 10/10 green after the baseline update. `npx tsc --noEmit`: 3 new errors introduced by this task's own test fixture (TS2698, spreading a `never`-cast object) found and fixed before completion; zero AdrEditors/AdrArtifactForm/AdrSupersedePanel-related errors remain (the tool's remaining ~60 errors project-wide are pre-existing across unrelated files, not touched by this task — spot-checked several, e.g. `node:fs` module-resolution noise in test files, unrelated to this wave).
- Not committed as of this task's own entry: per this task's explicit dispatch constraints ("Nicht committen — das übernimmt der git-Agent"), no `git add`/`git commit` was run by this task itself. `AdrForm.tsx`/`AdrForm.test.tsx` were removed with plain filesystem `rm` (not `git rm`) since `git rm` is blocked for a non-git-agent role by the orchestrator guard; the deletion was an unstaged working-tree change at that time. **Commit status as of the Task 23 entry below is UNKNOWN to this (Task 23) instance — check `git log`.**
- Open question for the next review pass (flagging, not resolving): whether `AdrSupersedePanel` living as a sibling rather than inside `ArtifactForm` should be the template for any future item-type-specific lifecycle action the remaining waves might turn out to need. **Task 23 finding: StakeholderNeed needed the same sibling-pattern for its `CustomFieldsEditor` (see Task 23 entry) — so this precedent has now been applied a second time, independently, to a different item type. Recommend treating "item-type-specific capability the shared renderer doesn't cover → sibling component next to the `*ArtifactForm`" as the established house pattern going forward, not a one-off.**

Task 23: complete (commit `841aec68`, 3 fix rounds — R-round bundled `985ea858`-style single commit `841aec68` covers ALL rounds since nothing was committed until final sign-off — sign-off review APPROVED_WITH_RECOMMENDATIONS, rating B) — **ROLLOUT WAVE 2c OF 7 (StakeholderNeed)**
- Fourth rollout wave: migrated `NeedForm` onto `ArtifactForm` (`NeedArtifactForm.tsx`), same base pattern as Tasks 19/20/21. `NeedForm` was the last live consumer of the old `AttributeVisibilityConfig` prop chain — deleting it removes that mechanism's final reader entirely. `DeriveRequirementsPanel` / manual "Anforderung ableiten" form / `TraceLinkPanel` moved up into `NeedsEditors.tsx` as siblings per the wave's explicit scope boundary (these are not attributes); moving them surfaced and fixed an incidental pre-existing dead handler (`onNeedsChanged` was called by the AI-derive accept-drafts flow but no caller ever passed it, so the needs list never actually refreshed after an accepted derive — now wired to the local `refresh()`).
- Ran through **4 total review cycles** (1 initial + 3 fix rounds, the last 2 explicitly human-approved beyond the pipeline's normal max-2-iteration loop) — by far the most iterations of any rollout wave so far, and instructive about a NEW failure class not seen in Tasks 19-21: **a review-driven fix round's own new code re-broke something, twice**, only caught by demanding a fully independent, fresh-eyes re-review each time rather than treating prior rounds' findings as the complete checklist. Chronology:
  1. **Cycle 1 findings (R-1/R-2/R-3 REQUIRED, S-1/S-2 should-fix):** `change_reason` handling was entirely missing from the plan's own literal reference snippet for `NeedArtifactForm.tsx`, despite `StakeholderNeedService.update_need()`/`delete_need()` genuinely enforcing it via `PresetPolicyService.is_change_reason_required()` on Extended-preset workspaces — same 100%-failure-class bug as Task 19's `uid` leak, but item-type-specific rather than shared-root-cause (confirmed: only `requirement_service.py`/`stakeholder_need_service.py`/`workflow_facade.py` call that check). Found and fixed BEFORE the first review by the implementer's own live verification. First review round then found: R-1 the old `NeedForm`'s `CustomFieldsEditor` (a pure client-side JSON editor) had no replacement, so `custom_fields` editing silently vanished from the UI even though `StakeholderNeedSerializer` genuinely persists it (verified: StakeholderNeed is a WORKING CONTROL for urgent gap #7, unlike Risk/Adr/Issue); R-2 the new `changeReason` local state wasn't reset on need-switch, so a reason typed for need A could leak into need B's PATCH; R-3 the claimed `ui-ratchet.test.ts` baseline edit had been silently overwritten by concurrent Task 21/24 edits to the same shared file (not this wave's bug, but its deliverable was genuinely missing); S-1 DELETE never sent `change_reason` (pre-existing bug, same gate as update, cheap to close alongside); S-2 `changeReason` wasn't wired into the dirty-check, so a typed-but-unsaved reason could be silently discarded on navigation.
  2. **Fix round 1** closed all 5: `CustomFieldsEditor` restored as a sibling in `NeedsEditors.tsx` (not folded into `ArtifactForm`, following the Task-21 `AdrSupersedePanel` sibling precedent — see the Task 21 entry's now-updated "open question"), `changeReason` reset via `useEntityReset(need.id, ...)`, `ui-ratchet` baseline re-measured fresh, DELETE now sends `change_reason` on Extended preset, dirty-check combined `formDirty || hasPendingChangeReason`. All 5 independently re-verified live by a second reviewer.
  3. **Cycle 2 (independent fresh review, not just a re-check of the 5 items) found a NEW regression the fix round's own code introduced:** N-1 REQUIRED — the restored `CustomFieldsEditor`'s draft state fed nothing into the dirty-check, so editing only a custom field and switching needs silently discarded the edit with no warning dialog — the EXACT bug class (S-2) the same round had just fixed for `changeReason`. Also N-2 SHOULD-FIX (latent, not live-reachable today) — the `custom_fields` PATCH became an unconditional overwrite rather than a merge, which was flagged as a theoretical future risk once a real `kind:"extended"` attribute ships.
  4. **Fix round 2** (human-approved beyond the max-2 loop, small/localized per the reviewer) closed N-1 (`customFieldsDraft` wired into `useFormDirty` against `need.custom_fields`) and, per the reviewer's own suggestion, ALSO "fixed" N-2 by turning the overwrite into a genuine 3-way merge (`{...need.custom_fields, ...values.custom_fields, ...customFields}`).
  5. **Cycle 3 (a SECOND fully independent fresh review, human pre-approved) found the N-2 "fix" was itself a new BLOCKER:** F-1 — because `StakeholderNeedService.update_need` REPLACES the whole `custom_fields` map server-side (no server-side merge), the new 3-way merge silently resurrected any key deleted or renamed in the sibling editor's draft, live-proven via a real GET→PATCH→GET round-trip (delete key `b` → PATCH → GET still shows `b`; renaming even accumulates orphan keys). The pre-N-2 overwrite had been correct all along — **N-2 was well-intentioned but wrong**, trading a hypothetical, not-live-reachable future problem for a live, one-click-reachable data-edit failure today. Also F-2 IMPORTANT — the dirty flag from N-1's own fix never cleared after a successful save (`handleSaved` never called `markCustomFieldsClean`), so the very next need-switch after ANY custom-field save showed a false unsaved-changes dialog.
  6. **Fix round 3** (human-approved, explicit direction: revert N-2 to the original overwrite, bundle the F-2 dirty-clear fix) reverted `custom_fields` to `patch.custom_fields = customFields ?? need.custom_fields ?? {}` (byte-identical semantics to the pre-N-2 state and to the deleted `NeedForm.tsx`'s own send-logic), added a binding removal-regression test (delete a key → PATCH must NOT resurrect it — the exact case every prior test suite missed, since all prior tests only asserted addition/override), and added `markCustomFieldsClean(customFieldsDraft)` to `handleSaved` before `refresh()`.
  7. **Final sign-off review (4th review cycle):** APPROVED_WITH_RECOMMENDATIONS, rating B. Independently re-verified `onSave` is genuinely a plain overwrite (not a merge or hybrid) via source read, live GET→PATCH→GET round-trips covering removal / add+override / rename / clear-all / untouched-fields (all correct), an independent dirty-clear probe including the specific interaction case explicitly asked for (failed save must NOT mark clean — confirmed it doesn't, since `markCustomFieldsClean` is only reached after `await update` resolves), and a full regression sweep of every prior round's fix (R-2/S-1/S-2/`_PROTECTED_PATCH_FIELDS`/edit-capability-vs-old-form/gap-#7-claim) — none had been quietly broken by a later round. 2 non-blocking recommendations left (a named regression-guard test for a #418 placeholder fix wasn't migrated; `CustomFieldsEditor` doesn't reflect a background refetch mid-edit, pre-existing across all its consumers) — see cleanup backlog.
- Live-verified throughout via real GET→real component pipeline→real PATCH/DELETE round-trips against the actual dev backend (DRF `APIClient`, same method as every prior wave), on BOTH Standard and Extended preset workspaces. Final `_PROTECTED_PATCH_FIELDS` diff: 0 leaks (PATCH body `{title, description, category, moscow_priority, custom_fields}` + `change_reason` on Extended only). `moscow_priority` null-clear works (`_UNSET` sentinels throughout `update_need`, no `is not None` trap). No field the old `NeedForm` could edit became uneditable.
- Files: created `frontend/src/components/NeedsEditors/NeedArtifactForm.tsx`, `frontend/src/test/NeedArtifactForm.test.tsx`, `frontend/src/test/needs-editors-derive.test.tsx`, `frontend/src/test/needs-editors-custom-fields-dirty.test.tsx`; modified `frontend/src/components/NeedsEditors/NeedsEditors.tsx`, `frontend/src/components/NeedsEditors/needs-editors.test.tsx`, `frontend/src/api/stakeholder-need.ts` (widened `update`'s `data` param to `Record<string, unknown>`, same deviation as Tasks 19/20's `risksApi`/`issuesApi`), `frontend/src/test/ui-ratchet.test.ts` (baseline, re-measured multiple times due to concurrent Task 21/24 edits to the same file — see cleanup backlog); deleted `frontend/src/components/NeedsEditors/NeedForm.tsx` + its 3 old test files (`need-form.test.tsx`, `need-form-change-reason.test.tsx`, `need-derive-requirements.test.tsx`), coverage migrated into the new test files (net 13 → 39 tests across the touched files).
- Commit `841aec68` on `feat/attribute-definition`, local-only (not pushed), staged explicitly by path (no `git add -A`) — excluded 2 pre-existing unrelated dirty files (`.claude/rules/use-orchestrator.md`, `.meta-config/project.yaml`) that predate this task and belong to neither this nor any other concurrent wave.
- Urgent gap #7 updated: StakeholderNeed is a genuine WORKING CONTROL for `custom_fields` (unlike Risk/Adr/Issue) — see gap #7 above.
- Corrected a stale ledger cross-reference: urgent gap #6 and the binding tracker previously hardcoded "Task 23" as the TestCase rollout wave; the real plan's Task 23 is StakeholderNeed. See the RESUME POINT correction note and the corrected gap #6/binding-tracker entries above.

## Standing runbook note (apply going forward)

- Frontend tasks: use the real `npm`/`vitest` tooling, IN THE CONTAINER.
- **Since Task 3:** ALL commits go through the `git` agent. Guard blocks even approved destructive git ops (e.g. `stash drop`) at the tool level - leave as a harmless no-op and flag it if this recurs.
- No standalone `task-N-brief.md` files pre-authored from Task 4 onward.
- **Since Task 4:** fix immediately if a later task would replicate the same bug; otherwise park with a named reason.
- **Since Task 6:** RLS-dependent behavior IS testable by spying on the tenant-arming call.
- **Since Task 7:** check blast radius against shared infra, verify layering claims against actual precedent.
- **Since Task 8:** hard, one-way Django data migrations default to higher review severity.
- **Since Task 9:** when retiring a legacy mechanism, check the FRONTEND callers of the old surface too.
- **Since Task 10:** any cache keyed narrower than the full authorization scope needs a WARM-cache probe.
- **Since Task 11:** trust the plan's own tests over its inline code snippets when they conflict.
- **Since Task 12:** verify REACHABILITY through the real production dispatch path before accepting a severity label.
- **Since Task 13:** attribute definitions are LIVE in every deployment since Task 8 - see the banner above.
- **Since Task 14:** check BOTH narrowing AND widening for any field-set/export/projection logic.
- **Since Task 15:** check live frontend callers of a changed backend API surface in the SAME review.
- **Since Task 16:** re-verify ANY suggested value/API shape against real source, including ones the CONTROLLER itself suggests.
- **Since Task 17:** a composite/multi-field UI component needs its VALUE SHAPE checked against the actual backend serializer contract, not just field names.
- **Since Task 18:** an integration/renderer task deserves a full adversarial end-to-end test against a REAL bootstrapped definition covering every feature combined, not just per-feature unit tests. Prefer re-anchoring on a true identity key over content-comparison heuristics when a house-style primitive for that purpose already exists.
- **Since Task 19 (applies directly to every remaining rollout wave):** "verified live" must mean rebuilding the ACTUAL payload the component emits (fetch a real GET, run it through the real form pipeline, inspect the actual PATCH body) - a hand-built minimal test payload can and did completely miss a 100%-failure bug. Every rollout wave should diff its real emitted PATCH keys against `_PROTECTED_PATCH_FIELDS` before being declared done. Also: fixing a bug at the shared root cause (here: the bootstrap introspection loop) rather than per-item-type is both less work and prevents the same bug recurring in each of the next waves - always ask "does this fix belong in the shared library/introspection code, or just in this one wave's file" before scoping a fix.
- **Since Task 20 (applies to every remaining rollout wave):** a clean `_PROTECTED_PATCH_FIELDS` leak-check is NOT sufficient live-verification on its own - also explicitly diff the introspected/editable attribute set against the OLD legacy form's field list (or the serializer's declared writable fields) to catch silent edit-capability regressions (fields that quietly stopped being editable, or a null-clear that gets silently swallowed by an `is not None` gate). Also: when a bootstrap-command field exclusion is GLOBAL (applies to all item types), verify it's actually correct for the item type in front of you, not just the one it was originally written for - the same column name (e.g. `severity`) can be genuinely derived on one entity and plain user-editable on another. And: whenever a `git`-agent delegation stashes files to keep them out of a commit, explicitly ask for stash-pop confirmation as part of the SAME delegation or a fast follow-up, don't assume it self-cleaned up.
- **Since Task 21:** an item-type-specific capability the shared `ArtifactForm` renderer has no concept of (a lifecycle action like ADR's Supersede-Flow, or a free-form editor like StakeholderNeed's `CustomFieldsEditor`, see Task 23) belongs in a sibling component next to the `*ArtifactForm`, not folded into the renderer or dropped. Read the FULL deleted legacy form before implementing a wave, not just the plan's inline code snippet — the plan's own snippets have twice now silently omitted a real, REQ-backed or backend-enforced capability (Adr's Supersede-Flow, StakeholderNeed's `change_reason` gate).
- **Since Task 23 (applies to every remaining rollout wave):** (1) a review-driven fix round's own new code can reintroduce the exact bug class it just fixed, or introduce an entirely new one — after ANY fix round, the next review must be a full fresh-eyes pass over the WHOLE diff, not a checklist re-check of only the findings that triggered the round, and must explicitly test interactions between the round's own multiple fixes (not just each fix in isolation). (2) When a sibling component (or any local state) needs to feed a shared dirty-check, both halves of the wiring matter equally: getting dirty MUST fire on edit, AND getting clean MUST fire on successful save — a fix that only does the first half creates a permanent false-dirty state, not a false negative. (3) For any `custom_fields`/free-form-map field: if the backend service does a wholesale REPLACE (not a merge) on save, the frontend's PATCH construction MUST also be an overwrite, never a merge against a stale baseline — a merge silently resurrects deleted keys and orphans renamed ones; test key REMOVAL specifically, not just addition/override, since every prior test suite in this project missed exactly that case. (4) On any file 2+ orchestrator instances edit concurrently without worktree isolation (here: `ui-ratchet.test.ts`), expect exact-equality assertions on it to flap for reasons unrelated to your own wave — re-measure fresh immediately before each commit rather than trusting an earlier measurement.
