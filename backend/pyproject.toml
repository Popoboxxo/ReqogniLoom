[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "reqflow.settings"
python_files = ["tests.py", "test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--strict-markers -v"
markers = [
    "slow: Performance-Tests, dauern > 1s",
]

[tool.coverage.run]
source = ["."]
omit = [
    "*/migrations/*",
    "*/tests/*",
    "manage.py",
    "reqflow/wsgi.py",
    "reqflow/asgi.py",
]
