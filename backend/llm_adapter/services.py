"""
ARCH-L1-009 LlmAdapter — Service stubs.

TODO(COMP-LA-001): Implement LlmService as the public entry point:
  - validate(artifact, workspace_id) -> ValidationResult
  - decompose(requirement, workspace_id) -> list[dict]
  - check_consistency(artifacts, workspace_id) -> ConsistencyReport
  All calls are routed through ResilienceOrchestrator (IF-L1-050).

Reference: docs/se/L1/Gesamtsystem/L2/LlmAdapterSystem/L2_LlmAdapterSystem_Architecture.md
"""
