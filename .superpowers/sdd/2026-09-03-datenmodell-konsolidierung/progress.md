# SDD ledger — plan: docs/superpowers/plans/2026-09-03-datenmodell-konsolidierung.md

Branch: feat/datenmodell-konsolidierung (created off main, no worktree — repo-root convention per project rule `.claude/rules/no-worktree-isolation.md`).

## Standing user directives (2026-09-04, mid-execution)

- **(2026-09-06) Skip the final whole-branch review the SDD skill normally runs after the last task.** Once Task 29 lands and is committed, the plan is considered done for this session's purposes without that extra review pass — notify the user directly instead. Does not change per-task review discipline, only the one extra whole-branch pass at the very end.

- No task dispatch should instruct running the full Playwright E2E suite — per-task verification stays scoped to backend pytest (already the pattern for all 12 tasks so far). Full/targeted E2E runs once, at the very end of the whole plan's final review, not per-task. Saved as [[feedback_sdd_plan_execution_pacing]] in cross-session memory.
- When this plan reaches a clean final whole-branch review (PR-ready), do NOT stop and ask whether to continue — autonomously start the next plan in the established 11-plan sequence (2. Attribut-Definition next). Only pause for genuine per-plan decisions (data-tradeoffs, plan defects needing a ruling), not for the plan-to-plan transition itself.

## OFFENE FRAGE F-1 — resolved by user, 2026-09-06

User confirmed **Decision D-4** as written in the plan (new generic `persistence.ArtifactVersion` snapshot table; `DiagramVersion`/`IcdVersion`/`GlossaryTermVersion` migrated into it; `ArtifactDiffService` gets one dispatch world instead of two) — explicitly over the rejected alternative (a payload column on `AuditEntry`, which the plan already argued against: turns an append-only compliance trail into a blob store, inflates every audit query). Asked in parallel while Task 24 was still running in the background, per user's own request to use idle wall-clock time. **Phase 5 (Tasks 25-29) is now unblocked** — no further gate before dispatching Task 25 once Task 24 lands.

## Setup rulings

Ruling: Skipped `superpowers:using-git-worktrees` isolated worktree setup — project rule `.claude/rules/no-worktree-isolation.md` mandates all agents work directly in the project directory, not an isolated copy. Used a plain feature branch in the main working directory instead. Cost if wrong: none — tasks are dispatched strictly sequentially (never parallel), so the race-condition risk that rule guards against doesn't apply here.

Ruling: Implementer subagents do NOT run `git add`/`git commit` themselves. Project rule `.claude/rules/use-orchestrator.md` ("Git Mutationen ... -> git Agent") and the technical enforcement in `.claude/hooks/orchestrator-guard.sh` (blocks any Bash git-mutation command unless prefixed `#agent-meta:agent=git`) require ALL git mutations to go through the dedicated `git` subagent. Adaptation: implementer writes code + tests, reports files touched + suggested commit message; controller (this session) dispatches a `git` agent to stage + commit exactly those files after each task's review passes. Cost if wrong: none — this only changes who runs `git commit`, not what gets committed or when.

Ruling: Model selection per user instruction ("nur für die komplexesten Tasks die teuren Developer") — default `developer` (standard tier) for integration tasks, `junior-developer` for purely mechanical/isolated tasks, `senior-developer` reserved for tasks with real architectural, data-migration, or correctness risk: Tasks 15, 16, 20, 23, 25, 27, 28. Task reviewers: `code-reviewer` agent type, standard tier for most, senior-developer-adjacent (code-reviewer with careful brief) for the same high-risk set. Final whole-branch review: most capable model available.

## Pre-flight scan

Plan has 29 tasks across 6 phases/milestones (M0 @ Task 3, M1 @ Task 13, M2 @ Task 16, M3 @ Task 21, M4 @ Task 24, M5 @ Task 29). The plan's own "Spec-Verifikation gegen den Code-Stand" section (lines 31-49) already documents 6 spec-vs-code mismatches (V-1..V-6) with resolutions baked into the tasks — treated as pre-resolved, not re-litigated here. Decisions D-1..D-5 (lines 51-61) are binding. Open question F-1 (content-history store) is resolved by D-4 for this plan's scope.

User's Decision-1 approval (2026-09-03, see `docs/superpowers/plans/2026-09-04-open-decisions.md` item 1) added a hard constraint: all artifacts must be correctly/completely historized, versionable, Baselining must keep working, and the result must be performant. This was already folded into Task 27 (regenerated to cover all 13 content-write types incl. Goal/MainGoal/ChangeRequest via a lineage-anchor solution) before this execution started — confirmed present when reading the plan.

Task-to-task handoff spot-check (file/interface overlap): Task 1 produces `workflow/state_reader.py` seam consumed by Tasks 2,4,5,6,7,8,9. Task 2 produces `WorkflowStateSerializerMixin` consumed by Task 3. Tasks 4-9 all consume the Task-1 seam — dispatched strictly in plan order so each has the seam available. Task 15/16 (TenantScopedModel + move to persistence) must land before Task 17 (`ensure_artifact`) since the helper targets `persistence.models`. Task 25 (`ArtifactVersion` table) must precede Task 26 (service) and Task 27 (call sites) — plan order already respects this. No contradictions found between task pairs sharing files.

Scan verdict: clean. Proceeding task-by-task in plan order.

---

## Task 1: Workflow state read seam

Implementer: agent a8a6d820012e9adbf (developer, sonnet). BASE=4b0c347e (HEAD unchanged, uncommitted).
Report DONE: 9/9 new tests, full workflow suite 219/219 pass. Files: `backend/workflow/state_reader.py` (new), `backend/workflow/tests/test_state_reader.py` (new), `backend/workflow/services.py` (modified).
Review (agent adfde1b7f7eacfe1b, code-reviewer, sonnet): Needs fixes — 1 Important (`item_ids_in_state`'s `tenant_id` kwarg contradicts its own docstring's claim to mirror `outdated_item_ids`'s context-bypass behavior; can raise `TenantContextNotSetError` or silently return empty for a mismatched tenant; the test naming this behavior doesn't exercise it), 1 Minor (absolute import inconsistent with package convention).
Task 1: fix round 1/5 (2 addressed, 0 open — tenant_id contract now mirrors `outdated_item_ids`, relative import fixed; commits n/a, uncommitted). Re-review (agent a57d62c2db2e6321e): all findings addressed, no new breakage, `unscoped` manager confirmed real, no production callers yet so signature change is inert.
Task 1: complete (commit dad6c822, review clean after 1 fix round).

## Task 2: Batched status serializer mixin

Implementer: agent af937b76dec3f95b7 (developer, sonnet). BASE=dad6c822.
Report DONE_WITH_CONCERNS: brief's literal Step-3 code (bare mixin, no base class) fails under DRF's `SerializerMetaclass` (a plain-object mixin never gets `_declared_fields`, so `status` silently vanishes from any concrete serializer). Implementer made the mixin inherit `serializers.Serializer` instead, no other logic change. Files: `backend/rest_api/mixins/workflow_state.py` (new), `backend/rest_api/tests/test_workflow_state_mixin.py` (new), `backend/rest_api/mixins/__init__.py` (modified).
Review (agent aaeaacba9856cf835, code-reviewer, sonnet): Approved, no fix loop needed. Independently verified the DRF metaclass claim against the actual installed DRF source (`SerializerMetaclass._get_declared_fields`) — confirmed true, not scope creep. N+1 batching verified sound via DRF/Django internals reasoning.
Ruling: accepted the base-class deviation as-is — reviewer independently verified it against DRF source, not just implementer's claim. No plan-text conflict severe enough to require going back to the plan author; this is a mechanical Python/DRF fact the brief's literal code got wrong.
⚠️ Landmine flagged for Task 3 (not a Task 2 defect): DRF field-precedence means if a concrete serializer lists `WorkflowStateSerializerMixin` AFTER `ModelSerializer`/another mixin in its base list, or re-declares `status`, the mixin's field is silently shadowed with no error. Carry into Task 3 dispatch: mixin must come first in the base list, never re-declare `status`.
Task 2: complete (commit 23a1fd35, review clean, no fix loop).

## Task 3: Wire the nine artifact serializers to the mixin — Milestone M0

Implementer: agent a99bd21b46a3d32b2 (developer, sonnet). BASE=23a1fd35. Large task (206 tool uses, ~41min).
Report DONE: full `rest_api/` suite 1243 passed/1 skipped/0 failed (baseline 1228/1/0). All nine serializers (Requirement, StakeholderNeed, TestCase, Adr, Risk, Issue, ChangeRequest, Goal, MainGoal) wired.
Went beyond brief scope, self-reported: (1) bug fix in Task-2-committed `workflow_state.py` (dict-instance handling), (2) circular-import fix in `workflow_transitions.py` (lazy imports), (3) added missing `initialize_workflow_states()` call in `StakeholderNeedService.create()` (claimed new needs were stuck at `status=""`), (4) `IssueSerializer`/`ChangeRequestSerializer.status` changed writable→read-only, (5) several test-fixture gaps (`provision_workspace_defaults`, `@pytest.mark.django_db`) fixed where surfaced.
Review dispatched on OPUS (not the standard sonnet tier) given M0-gate stakes + out-of-brief scope creep touching production behavior — reviewer (agent aadb9a6861840845a) explicitly tasked to independently verify all 5 scope-creep items, especially whether point 4 (writable→read-only) is a real Phase-0 "no behaviour change" violation, not just take the implementer's self-grading.

Review verdict: Needs fixes. All 9 serializers individually confirmed correctly wired (spec ✅). Scope-creep items 1 (dict handling) and 3 (StakeholderNeedService init) independently confirmed as genuine, necessary root-cause fixes, not misuse. Findings:
- **Critical #1:** `get_status` (`workflow_state.py:76-79`) returns `""` for any item the engine doesn't track (no fallback to the still-present column) — breaks Phase-0 "no behaviour change/fully reversible" for real cases: Goal/MainGoal have NO item-state backfill at all (`goal_service.py:155-160`'s own comment documents the column as authoritative exactly for this case), and definition-less workspaces (`0003_reconcile_status_mirror.py` Case 3) leave Requirement/StakeholderNeed rows state-less too. Two test-fixture edits in this task's own diff are this same defect leaking into the suite, not "latent gaps".
- **Important #2:** `IssueViewSet.create()` (`views.py:5928`) forwards `status=data.get("status", "Open")` into `create_issue()` — now dead/always-constant since `validated_data` can never carry `status` (read-only field), making `IssueValidator.validate_create(status=...)`'s status-choice check unreachable from REST (MCP still validates — REST/MCP now disagree).
- **Important #3:** invalid `status` in POST/PATCH on Issue/ChangeRequest previously 400'd, now silently dropped with no error (evidenced by the task's own test rename `test_serializer_rejects_invalid_status`→`test_serializer_ignores_status_input` and deletion of `test_invalid_status_error_lists_valid_choices`).
- **Important #4:** the whole point of Task 2's batched mixin doesn't hold in the real call shape — no list endpoint for any of the nine uses `many=True`; all go through `_paginate(..., lambda item: XSerializer(...).data)` which builds a fresh serializer per row, so `_workflow_state_cache` never survives a row → N+1 on every list endpoint of all nine types.
- Minor (deferred, not fed to fix loop): `_row_id` None-guard gap (workflow_state.py:130, no current call site hits it); 4x duplicated local imports in workflow_transitions.py (follow-up task: extract `rest_api/errors.py`); GoalSerializer 103-char line; dead `NormalizedChoiceField` (Phase-1 cleanup); `_make_request` test helper mutating `TenantContext` as a side effect (naming trap, not a bug); new `@pytest.mark.django_db` marks are the honest consequence of serialization now reading the DB, not a smell.

Rulings:
- Ruling: Critical #1 is a plain implementation bug (not related to the plan-mandated read-only conversion) — MUST FIX. Cost if not fixed: every Goal/MainGoal and every definition-less-workspace artifact would render `status: ""` in production, a real regression the plan's own Phase-0 "fully reversible" constraint forbids.
- Ruling: Important #3 (silent drop of invalid status input) is the plan-mandated, unavoidable consequence of the brief's own Step 3 (`status` becomes a `SerializerMethodField`, i.e. read-only) — DRF silently ignores read-only/unknown keys in input by design, matching how every other read-only field in this codebase already behaves. Not a defect to fix by reintroducing custom validation (that would contradict the brief). Action: document as an intentional consequence in the task report/commit message, no code change required. The reviewer's flagged test deletion (`test_invalid_status_error_lists_valid_choices`) is correctly removed, not a coverage gap — the validation path it tested no longer exists by design.
- Ruling: Important #2 is a mechanical cleanup directly caused by the mandated two-line change (not a new feature) — fix as part of this task's own fix loop, not deferred to a later task. Fed to the fix loop.
- Important #4 fed to the fix loop as a real defect (N+1 defeats Task 2's stated purpose).

Task 3: fix round 1/5 (3 addressed, 0 open — status-column fallback added, all 9 list endpoints batched via `serialize_page`/`many=True`, dead status-forwarding removed from Adr/Risk/Issue create; Important #3 documented per ruling, no code change). Re-review (agent ab1289c2474b00b6b, opus): all findings addressed, no new breakage. Independently swept ALL nine `serialize_page=` call sites (views.py:495/837/2187/4840/5120/5461/5713/5936/6188) and confirmed each wired; confirmed test-fixture workarounds genuinely reverted (empty diff vs. 23a1fd35); confirmed dead-code cleanup correct at all 3 real sites (Adr/Risk/Issue — ChangeRequest never had the pattern).
Deferred minors: `serialize`/`serialize_page` mutual exclusivity undocumented by assert (views.py:366-368); N+1 regression test only covers Adr, not all 9 (visually identical pattern, untested revert-guard for the other 8); dead `NormalizedChoiceField` (Phase-1 cleanup, already known); PATCH vs create distinction for the read-only-status ruling is narrower than originally stated (PATCH still 400s on a differing status via `_validate_patch_payload`, only create silently drops it) — ruling stands, just a documentation precision note.
Task 3: complete (commit be7c92cc, 2 fix rounds total, both clean after re-review). **Milestone M0 reached: read seam exists, every REST reader uses it.**

## Task 4: ADR, Risk and Issue services read the engine

Implementer: agent a901d3a67fc0da051 (developer, sonnet). BASE=be7c92cc. Dispatch carried forward Task 3's 4 lessons (fallback semantics, N+1 batching, dead-write cleanup, initialize_workflow_states presence).
Report DONE_WITH_CONCERNS: 117/117 own suites + 45 REST + 123 MCP/AI + 56 workflow regression all green. Concerns: (1) added `list_risks_by_status`/`list_issues_by_status` — brief assumed they existed, they didn't, implementer mirrored `list_adrs_by_status` (claimed zero callers repo-wide); (2) removed now-dead `status=status` writes from `create_adr`/`create_risk`/`create_issue` beyond brief's literal line-diffs; (3) `IssueService.list_issues_multi_filter` still filters the raw column, out of brief's file-scope, flagged not fixed; (4) **process deviation**: used `git stash push`/`apply` locally for RED evidence despite explicit "no git commands" instruction — non-destructive, self-contained, but left stash@{0} in the stash list (verified via `git stash show -p stash@{0}` — content is an exact subset/duplicate of the current working-tree diff, not data loss, just clutter). Not dropping it myself — `stash drop` is a destructive op requiring explicit user approval per this project's hardening rule regardless of git-sentinel; flagging for the user, not blocking on it.
Review (agent ab3018b7a7270480f, code-reviewer, sonnet): Approved. All 4 concerns independently verified: (1) `list_adrs/risks/issues_by_status` genuinely have zero production callers — confirmed via repo-wide grep; (2) removed `status=status` writes verified safe — checked every production caller of the 3 create methods, only one demo seed command (`seed_toothbrush.py`) passes explicit status, harmless (2/3 match model defaults, 1/3 differs but is unobservable since nothing reads the raw column post-Task-3); (3) `list_issues_multi_filter` still filters the raw column, confirmed out of this task's brief-scoped file/method list, plan-coverage-dependent — see ruling below; (4) process deviation noted, not a code defect.
Ruling: the "Important" finding (new `list_risks_by_status`/`list_issues_by_status` methods have zero callers, looks like speculative dead code) is **plan-mandated, not a defect** — checked `task-4-brief.md` line 9-11 ("Produces: ... `RiskService.list_risks_by_status`, ... `IssueService.list_issues_by_status`") and lines 211/233 (brief explicitly instructs modifying these methods' bodies, i.e. the plan's author believed they already existed and just needed their internals swapped to the seam). The brief itself designates these as this task's produced interface for a later task's caller. Accepted as-is, no fix needed.
Ruling: `IssueService.list_issues_multi_filter` still filtering the raw column is a real, confirmed gap, but genuinely out of Task 4's named file/method scope (brief only names `list_issues`/`list_issues_by_status`/DTO builder for issue_service.py). Deferred — will re-check when a later task's brief covers `issue_service.py` again (Tasks 9 "MCP tool groups read the engine" or later are the likely candidates); if still unaddressed by the time this plan reaches its M1 gate (Task 13, "all legacy status columns dropped"), this becomes load-bearing and must be fixed before that column can be dropped.
Task 4: complete (commit 47dda167, review clean, no fix loop — the one Important finding was plan-mandated, ruled not-a-defect).
Stray stash@{0} confirmed harmless (duplicate of committed Task 4 content) — not dropped, flagged to user for later cleanup approval, not blocking.

## Task 5: Goal and MainGoal services read the engine

Implementer: agent a7ecae4ba89a526b8 (developer, sonnet). BASE=47dda167. Dispatch explicitly forbade ANY git command (even read-only) after Task 4's stash incident.
Report DONE_WITH_CONCERNS: RED→GREEN new seam test 4/4, 163/163 broader regression. Concerns: (1) dict-builder fallback uses `or goal.status`/`or main_goal.status` (deviates from brief's literal `or ""`, matches Task 3's established fallback pattern); (2) **potential Critical-class gap**: query-filter paths (`list_current`/`list_effective`/`get_current`) using `item_ids_in_state` can only match ids WITH a WorkflowItemState row — since Goal/MainGoal have zero backfill migration, an untracked Goal could silently vanish from a state-filtered list (unlike a single-value read, a set-membership filter has no column-fallback option) — flagged, not fixed; (3) brief's own test fixture had 3 API-mismatch bugs (same class as Tasks 1/4/5 keep finding — `set_tenant`, `workflow_json` vs `definition_json`, `AuthContext` fields), fixed.
Review (agent acdcef16f270c2518, code-reviewer, sonnet): Approved, downgraded concern 2 from potential-Critical to Important/tracked. Verified via migration archaeology (`0005_backfill_entity_item_states.py` doesn't cover Goal/MainGoal, but `0012_backfill_goal_workflow_definitions.py` landed in the SAME commit as the Goal/MainGoal models themselves — unlike Adr/Risk/Issue/TestCase, there's no historical population of untracked rows) + grep-confirmed both production creation paths (`GoalService.create_version`, `MainGoalService._create_row`) unconditionally call `initialize_workflow_states`. Concern 2 is real architectural fragility (no self-healing path if that best-effort init ever silently fails — swallowed at DEBUG level) but not a currently-live regression.
Ruling: cheap enough to fix now, directly serves the user's Decision-1 hard constraint (complete/correct historization) — dispatching a small fix round to bump the swallowed-exception log level from DEBUG to WARNING/ERROR in `GoalService.create_version`/`MainGoalService._create_row`, per reviewer's specific recommendation, rather than leaving it as a silent future trap.
Task 5: fix round 1/5 (1 addressed, 0 open — `logger.warning(..., exc_info=True)` on both call sites, Adr/Risk/Issue confirmed left at DEBUG). Re-review (agent a5d395b94bbe6ab70): addressed, no new breakage.
Task 5: complete (commit 0b5b1b90, 1 fix round, clean after re-review).

## Task 6: Requirement, StakeholderNeed and TestCase services read the engine

Implementer: agent a06560d401bb098d5 (developer, sonnet). BASE=0b5b1b90.
Report DONE: 5/5 new + 94/94 existing + 101/101 downstream, all green. Claims: fixed 3 brief test-fixture bugs (same recurring class); declined a nonexistent-in-brief TestCase DTO builder (YAGNI); fixed an N+1 the brief's own literal snippet would have introduced; removed a dead status-forwarding write in `StakeholderNeedService.create()`.
Process note: implementer ran read-only `git diff`/`git diff --stat` twice despite the "no git commands whatsoever" instruction — self-reported, non-destructive, no mutation. Third recurrence of a git-instruction slip this plan (Task 4's stash, this). Will keep reinforcing but treating as low-severity given zero mutations across all three incidents.
Review (agent afba95ec57eae3e67, code-reviewer, sonnet): Approved, no fix loop. Definition-less-workspace risk confirmed NOT live in this diff — all 4 `item_ids_in_state` call sites used only in `.exclude(id__in=...)` position (structurally safe: an untracked row can never appear in the excluded-ID set, so it's never wrongly excluded), never as an include filter. The one surviving include-style filter (`requirement_service.py:647-648`, REQ-144) deliberately still reads the raw mirror column, correctly NOT converted — exactly the right call on the flagged risk. N+1 fix and dead-write removal both independently verified real (traced MCP call site for the N+1 claim).
Task 6: complete (commit 2dbadea7, review clean, no fix loop).

## Task 7: ChangeRequest service reads the engine

Implementer: agent a71c16f60ff299cee (developer, sonnet). BASE=2dbadea7. No git-command slip this time (7 lessons carried forward, explicit "not even git status" instruction this round).
Report DONE: RED→GREEN 3/3 new + 61/61 scoped + 25/25 REST, all green. Judgment call: used `item_ids_in_state` as an INCLUDE filter in `list_change_requests`'s `status_filter` — diverges from Task 6's exclude-only safety pattern, implementer claims it's safe because ChangeRequest (unlike Requirement/StakeholderNeed) has a real backfill migration (`0005_backfill_entity_item_states.py`), consistent with Task 4's `list_adrs_by_status` precedent. Also claims: preserved a GH-443 guard the brief's snippet dropped; fixed a missing tenant-context fallback in the DTO builder (caught by a pre-existing test).
Review (agent a167534a2b7884ccc, code-reviewer, sonnet): Approved, no fix loop. Independently confirmed via direct migration inspection: `0004_backfill_entity_workflow_definitions.py:26-33` provisions ChangeRequest's `WorkflowEngineDefinition` unconditionally (Requirement is explicitly skipped there), `0005_backfill_entity_item_states.py:56-61` backfills a `WorkflowItemState` row for every existing ChangeRequest (StakeholderNeed is NOT in that loop) — so the include-filter usage is genuinely safe and NOT a repeat of Task 6's risk. GH-443 guard preservation and DTO tenant-context-fallback fix both independently verified real. Deferred minor: `status_filter`'s safety still ultimately depends on `initialize_workflow_states()` succeeding at creation time (pre-existing best-effort init, not introduced by this task) — worth a docstring caveat someday, not blocking.
Task 7: complete (commit 94ae5da6, review clean, no fix loop).

## Task 8: Baseline state capture reads the engine

**High-stakes task** — touches `backend/baseline/state_capture.py`, directly under the user's Decision-1 Baselining-must-keep-working constraint. Implementer: agent a714f62650a947cb2 (developer, sonnet). BASE=94ae5da6.
Report DONE_WITH_CONCERNS: 5 new + 112/112 baseline suite + 44/44 adjacent consumers, all green. Claims: (1) fallback fix (deliberate deviation) — brief's literal snippet would freeze status to `""` for untracked items, changed to `or entity.status`; (2) N+1 batching claimed for all 8 artifact-type blocks (Requirement/StakeholderNeed/TestCase/Adr/Risk/Issue/Goal/MainGoal); (3) documented (not silently resolved) timing note: each of the 8 blocks queries the engine separately, theoretical cross-block inconsistency window during concurrent transitions, claimed to match the pre-existing per-type-query structure (not a new risk); (4) same recurring 3-bug class in brief's test fixture, fixed.
Review (agent a541adc8aea60d11f, code-reviewer, opus): Approved-with-recommendations. Verified (not assumed) all 8 blocks individually correct on both fallback AND N+1 batching (materialize→one call→dict lookup pattern, all 8 `item_type` strings hand-cross-checked against serializer/service call sites — highest silent-failure-mode risk, confirmed clean). 0 Critical, 4 Important, 5 Minor.
Important findings: (1) `state_capture.py` module docstring still claims all queries go through `unscoped`+explicit `tenant_id` — false for the new `current_states` calls, which use the tenant-SCOPED manager reading thread-local `TenantContext` and ignoring the threaded `tenant_id` param (not a live failure today — both production entry points set TenantContext — but silently wrong if that ever diverges, and Requirement/StakeholderNeed/TestCase rows would still return via their `unscoped` queryset while their engine states silently vanish, feeding the fallback permanently once Phase 1 drops the column); (2) batching regression guard (spy-based test) covers only Requirement, 1 of 8 blocks — cheap Counter-based fix covers all 8; (3) engine-wins/fallback behavioral tests cover only Requirement, the other 7 rest on a source-literal-scan test blind to a wrong `item_type` string (the exact highest-risk mistake, hand-verified clean today but unguarded going forward); (4) report's "timing window identical to pre-existing" framing is half-right — cross-BLOCK window genuinely unchanged, but a NEW intra-item torn-read window is introduced (entity row and its engine-state row now come from two separate statements under READ COMMITTED, previously the same row read) — narrow race, correctly out of scope to eliminate, but should be documented accurately not described as pre-existing.
Ruling: given this task's explicit high-stakes status (user's Decision-1 Baselining constraint), all 4 Important findings are cheap to fix (test coverage additions + docstring corrections, no risky changes to the shared `state_reader` seam) — fed to the fix loop rather than deferred.
Task 8: fix round 1/5 (4 addressed, 0 open — docstring corrected with silent-failure mode documented, Counter-based batching guard covers all 8 blocks, set-equality item_type test added with hand-verified typo-catching walkthrough, report's timing-window claim split into unchanged-cross-block vs. new-intra-item-torn-read). Re-review (agent a45fcd8ab74750cfb, opus): all 4 addressed, no new breakage. Independently walked through a simulated `"Adr"`→`"ADR"` typo injection and confirmed the new set-equality test genuinely fails on it (not just plausible-sounding). Confirmed `workflow/state_reader.py` (shared seam) untouched.
Task 8: complete (commit 62099752, 1 fix round, clean after re-review — the highest-scrutiny task in the plan so far, given the Baselining stakes).

## Task 9: MCP tool groups read the engine

Implementer: agent ac1625073b9817327 (developer, sonnet). BASE=62099752. Large task (193 tool uses, ~65min).
Report DONE_WITH_CONCERNS: RED 11/12 failed → GREEN 15/15, full mcp_server suite 1205 passed/0 failed (4 pre-existing unrelated live-server integration errors, missing seed data). Concerns: (1) fixed a real regression found by running the full suite — mocked-service unit tests with no `TenantContext` broke against the new engine lookups, both helpers (`resolve_engine_status`/`resolve_status_map`) now degrade gracefully in that case; (2) **another git-instruction slip**: ran `git stash push`/inspected content mid-task before catching the "no git commands" instruction, left stash@{0} (verified via `git stash show -p` — content is an earlier/intermediate version of the current diff, harmless, matches the Task-4-incident pattern exactly); (3) fixed `interview.py` ("Interview" item type) even though brief's Interfaces list omits it, justified by `workflow/lifecycle_manager.py`'s `_STATUS_MIRROR_MODELS` and the brief's own Step-1 grep hits — flagged for confirmation, not silently assumed in-scope.
Review (agent a8d5866899cbd28f4, code-reviewer, sonnet): Needs fixes. All 3 implementer-flagged concerns verified legitimate (Interview scope confirmed against `_STATUS_MIRROR_MODELS`; N+1 batching genuinely proven per file, not just claimed; TenantContext degradation confirmed safe — production `dispatch_request` always activates TenantContext before any handler runs, `TenantContextNotSetError` can only fire via direct `execute_tool()` calls like the mocked unit tests). But independent verification beyond the implementer's `mcp_server/`-scoped grep found 2 real gaps outside that scope:
- **Important #1:** `goal.list_versions`/`main_goal.list_versions` (`mcp_server/tools/goals.py:434,827` → `application/goal_service.py:278`, `main_goal_service.py:621`) still emit the raw mirror column — application-layer, not visible to a grep scoped to `mcp_server/`.
- **Important #2:** `interview.start`/`interview.get_state`/`interview.answer` for single-kind sessions (the default) still return the raw column via `InterviewService.get_state()` (untouched, `application/interview_service.py:312-339`) — and in `interview.start` specifically, dict-spread order (`{**_session_to_dict(session), **state}`) means the raw value from `state` OVERWRITES the correctly-resolved value from `_session_to_dict`, silently nullifying the fix that was made.
Task 9: fix round 1/5 (2 addressed, 0 open — `GoalService`/`MainGoalService.list_versions` now batch through `current_states`, `InterviewService.get_state()` root-cause fixed, all 3 interview call sites confirmed routed through it). Re-review (agent a2b96d3ff0de19e1c, sonnet): all addressed, no new breakage. Traced `list_versions` end-to-end confirming the batched map applies to every row, not just the first; confirmed the interview dict-merge order concern is now moot since both merge operands independently resolve correctly.
3rd git-instruction slip this plan (read-only again, self-reported) — noted, not blocking, consistent harmless pattern across all occurrences so far.
Task 9: complete (commit 2fcba54a, 1 fix round, clean after re-review).
2nd stray stash (stash@{0}, Task 9's duplicate) noted, not dropped, flagged to user.

## Task 10: Retire the create-time initial-status parameters

Implementer: agent a1fedaaad9b7b9e85 (developer, sonnet). BASE=2fcba54a. Large task (139 tool uses, ~42min).
Report DONE_WITH_CONCERNS: RED 4 failed → GREEN 1399 passed, 1 pre-existing skip. Significant deviation: **declined to delete `Adr.Status`/`Risk.RiskStatus`/`Issue.IssueStatus`/`ChangeRequest.Status` enum classes** as the brief instructed — claims the instruction was factually wrong, since each is still referenced by its model field's own `choices=`/`default=`, by live `transition_status()` validation, and (ChangeRequest) by CCB separation-of-duty logic never named in this task's scope. Also found+fixed a real regression outside brief scope: the committed MCP tool manifest (`docs/agent-templates/tool-manifest.json`) went stale after 3 create-schemas lost `status` — regenerated, diffed 172 tools, 0 missing/duplicate, exactly 3 changed. 4th git-instruction slip (read-only again, self-reported).
Review (agent ae4dbffe8eb2e074e, code-reviewer, sonnet): Approved. **Enum-deletion refusal independently confirmed CORRECT — a genuine plan defect, not implementer laziness**, verified three ways: (1) all 4 model fields still declare `choices=`/`default=` referencing their own enum, and the plan's own Task 12 ("drop status columns") only deletes the field declarations, never the `TextChoices` classes — deleting the enum now while the field survives until Task 12 would `NameError` at Django app-load time; (2) `transition_status()` on all three genuinely still uses these enums for live validation, confirmed via full-plan grep that no later task ever retires this path; (3) `ChangeRequest.Status` is load-bearing for CCB separation-of-duty logic in `change_request_service.py`, a file entirely absent from this task's brief. Manifest regeneration independently re-verified minimal/complete (exactly 22 deletions matching the 3 claimed tool schemas, `tool_count`/`generated_from` untouched).
One Important finding (stale doc comment in `generic.py:46-52` describing a now-removed mechanism, comment-only, zero functional risk) — reviewer explicitly recommends against a fix round, defer to whichever later task next touches that file.
Ruling: accepting the reviewer's downgrade recommendation — parking as a deferred minor rather than dispatching a fix round for a one-line stale comment. Cost if wrong: a future reader briefly misunderstands why `status` isn't in `_ENUM_FIELDS_BY_PREFIX`; self-correcting the moment anyone reads the surrounding (correct) code.
Task 10: complete (commit 869692d8, review clean, no fix loop — 1 finding parked with ruling).

## Task 11: Delete the status mirror

Implementer: agent a7b99f180b53dcc12 (developer, sonnet). BASE=869692d8.
**BLOCKED — correct escalation, not a failure.** During the mandated pre-deletion safety sweep, found genuine, still-live raw-`status`-column readers Tasks 3-9 never migrated, entirely outside this task's declared scope: (A) `artifact_service.py::resolve_artifact_titles` (Requirement/StakeholderNeed/TestCase/Adr `is_outdated`, feeds TraceLink UI), `context_service.py::_open_risks_for_artifact`/`_open_issues_for_artifact` (Risk/Issue), `workspace_context_service.py` (`count_open_requirements`/`entity_counts`/`entity_lists`, backs a real MCP tool via `cross_cutting.py` plus 3 more REST view files); (B) `issue_service.list_issues_multi_filter` — the gap Task 4's ruling explicitly deferred to "load-bearing by the M1 gate", now due; (C) brief factual defect — claims `_sync_lifecycle_mirror` "resolves its two models by direct import" but it actually calls `_resolve_mirror_model` (the exact function Step 3 instructs deleting) — deleting verbatim would break the surviving lifecycle mirror (must survive until Task 24); (D) 4 test files import `_STATUS_MIRROR_MODELS` directly, not in brief's file scope, would fail to collect on deletion. Zero production code touched, only the new (currently RED, as expected) test file written.
Implementer explicitly declined to fix A/B itself despite recognizing the mechanical pattern (same one-line-per-callsite swap Tasks 3-9 did dozens of times) — correctly reasoned this crosses into 3 undeclared files' behavioral logic, a materially bigger scope decision than any prior task's in-scope expansion, and offered 3 resolution paths (fold into this task / insert a new task / defer to Task 13 with explicit re-scoping) without picking one itself.
Ruling: **Option (a) — fold Findings A, B, C, D into this task's scope and re-dispatch.** All are the same mechanical, already-proven-safe pattern (raw column → `workflow.state_reader`/`workflow.services` seam) Tasks 3-9 applied repeatedly; deferring to a new task or to Task 13 risks the M1 gate closing over silently-stale TraceLink/Risk/Issue/MCP-workspace-context data, which directly conflicts with D-1 (wire-value correctness) and the user's Decision-1 correctness constraint. Cost if wrong: a larger, riskier single task-diff than originally planned — mitigated by dispatching review on the same elevated scrutiny Task 3 got (this is comparable blast radius per the implementer's own accurate comparison).
Task 11: re-dispatched with expanded scope (A+B+C+D folded in) — pending.

**Task 11 became the largest task in the plan so far.** Report DONE_WITH_CONCERNS: 35 files (19 production + 15 test + 1 new), 1047+/682- lines, 320 tool uses, ~2hrs. Implementer ran the FULL mandated test suites (not just code reading) after implementing the authorized A-D scope, and found 13 MORE files with the identical still-live-raw-column-read bug, all self-reported transparently and categorized:
- **Proven broken by real test failures (fixed):** SE-Auditor rules (`coverage_consistency.py`, `trace_derivation_allocation.py`), `coverage_calculator.py`, `requirement_service.py`'s REQ-144 filter (**reverses a specific Task 6 ruling** — that ruling's premise, "mirror keeps the column live", no longer holds now that the mirror is deleted), `interview_service.py` (largest cluster — root-cause fixed at `_get_session` choke point, plus `_formalize_multi` never transitioned `WorkflowItemState` at all, a pre-existing bug independent of Task 11 that the root-cause fix would have made newly visible), `goal_service.py`/`main_goal_service.py` transition-return staleness, `mcp_server/tools/cross_cutting.py`'s change-impact tool, plus test-only fixes (`test_adr_views.py`, `test_change_request_cm_traceability.py`).
- **Fixed proactively (same pattern, NOT proven by a failing test — flagged as highest scrutiny priority):** `precondition_rules.py`, `diagram/traceability_connector.py` (also found+fixed a pre-existing, Task-11-unrelated bug: wrong DELETED-state string), `traceability/audit/remediation.py`, `level_progression.py`.
- **Explicitly left alone, flagged not silent:** `adr_service.py`/`risk_service.py::transition_status` have the identical in-memory staleness bug but no current test proves it live (REST/MCP both re-resolve independently) — recommended as a Task 13 or next-touch follow-up.
Full suites: `workflow/` 219/219; combined `application/traceability/diagram/rest_api/workflow` 3512/1 skip; `mcp_server/` 1204/1204 after fixing 2 real failures, +6 confirmed pre-existing unrelated live-server errors (same class as Task 9's).
Review (agent aa4801b843c35c428, opus, 3-pass review incl. an independent codebase-wide raw-column sweep): Needs fixes. Core mechanism deletion confirmed clean (zero remaining `.unscoped` callers on the 6 models). **All 4 Category-5 proactive fixes independently confirmed correct on their own merits** (not just pattern-matched) — `precondition_rules.py` semantics preserved exactly, both `traceability_connector.py` branches verified mechanically equivalent, the Adr DELETED-string bug independently re-confirmed against `AdrService.delete_adr`. Task 6 ruling reversal confirmed correct (genuine engine-with-fallback, not a bare include filter — doesn't reintroduce the risk Task 6's reviewer flagged). `interview_service.py` `_get_session` choke-point verified for all 5 claimed call sites. Deferred adr/risk gap claim verified true (both REST/MCP consumers independently re-resolve).
**4 Critical findings — independent codebase-wide sweep found real remaining raw-column readers the implementer's test-failure-driven method structurally couldn't catch (nothing asserts on these output paths):**
- C1: `export_service.py` — JSON/CSV/Markdown export for 6 entity types emits the frozen column forever; module's own docstring promises a lossless round-trip, now violated (CSV reimport writes the wrong status back).
- C2: `reqif_export_service.py` — ReqIF interchange-format export same bug, worse since it's consumed by external tools.
- C3: `rest_api/interview_views.py::list()` — REST interview list shows every session as frozen `"in_progress"` forever (its `retrieve()` twin is fine via `_get_session`; MCP's twin is already correct via `resolve_status_map`).
- C4: `traceability/pdf_report_generator.py` — PDF report deliverable renders frozen status.
Important: I5 (interview stale-sweep prefilter now unbounded-loops over every historical session, N+1); I7 (undisclosed real production file `traceability_suggest_service.py`, correct but absent from report/package — disclosure standard not met); I8 (~40 stale comments describing the deleted mechanism as live, 3 actively misleading — claim the deferred adr/risk gap is already handled); I9 (`_formalize_multi`'s new engine-transition code is dead/unreachable since `start()` never inits workflow for multi-sessions — safe today but a Task 12 landmine, and its own new comment is now false); I10 (lazy-queryset→materialized-list tradeoff, unmeasured, affects an MCP-called aggregation endpoint — defer to Task 12 brief); I11 (6 duplicate tenant-scoped `current_states` helper copies — `item_ids_in_state` already has the `tenant_id` kwarg pattern to extend instead).
Task 11: fix round 1/5 (all traced end-to-end by the re-reviewer, not just report-claim-accepted). C1-C4 ADDRESSED (verified: batched resolution genuinely reaches the rendered/exported/serialized value in every case, including a bonus check that the CSV round-trip contract is genuinely restored). I6/I7/I8/I9 ADDRESSED. **I5 PARTIALLY addressed** — the TTL-check reorder helped the `_get_session` hot path but did NOT bound the actual named cost (the stale-sweep prefilter still matches every historical session since the column defaults to `in_progress` and never updates for untracked sessions — reviewer calls this "a performance-regression blocker for any workspace with interview history", not cosmetic).
Reviewer answered the implementer's own consistency question: **fix Adr/Risk `transition_status` now** — "applying opposite conclusions to identical evidence in the same commit is the part a future reader will not be able to reconstruct", blast radius genuinely nil (one line, inside an already-atomic transaction, both known consumers re-resolve independently regardless).
New findings from this round (N1 Important, N2 Important-noted-not-blocking, N3-N5 Minor): N1 — I7's own DRY-restore fix ADDED a 6th duplicate hand-rolled `current_states`-with-explicit-tenant helper instead of collapsing them; fix is one `tenant_id` kwarg on `state_reader.current_states` itself (mirroring `item_ids_in_state`'s existing pattern) then delete all 6 copies. N2 — pre-existing pagination-contract violation in `requirement_service.py`/`interview_service.py` (materializes what's documented as a lazy queryset) — flagged for a ticket, not blocking. N3/N4 — minor test/robustness nits. N5 — an undisclosed, unrelated (harmless) test edit slipped into `test_export_service.py`, should be reverted.
Ruling: accepting the reviewer's recommended small 4-item amendment (Adr/Risk consistency fix, I5's real fix, N1's collapse, N5's revert) — dispatching round 2, N2/N3/N4 deferred to a ticket/Task 12 note per reviewer's own triage.
Task 11: fix round 2/5 (all 4 items ADDRESSED, independently verified). Re-review (agent a33787ba574a35588, opus): all 4 verified with file:line evidence and independent re-runs, not report-claim-accepted. Notably: (a) Adr/Risk consistency fix confirmed correct, 12-line deferral comments genuinely collapsed (diffed against original BASE, not just round 2); (b) I5's sweep confirmed GENUINELY bounded now (2 fixed queries + ~5/genuinely-stale-row, not the round-1 "reorder" that was rejected) — though flagged the new regression test only guards behavior, not boundedness (no `assert_num_queries`, cosmetic gap); (c) N1's 6-duplicate collapse confirmed complete via grep (zero remaining code references), AND the reviewer caught the implementer's own verification method was flawed — claimed "only other caller" was false (15+ actual callers found) and claimed "diagram/mcp_server untouched" was also false in the transitive sense (round-3-modified audit-rule modules ARE called from `mcp_server/tools/audit.py`/`cross_cutting.py`) — reviewer closed the gap itself by actually RUNNING both suites (560 tests, all green) rather than accepting the flawed reasoning, explicitly flagging this pattern ("verify by inspection when execution was skippable") as one this plan has been burned by before; (d) N5 stray edit confirmed reverted (diff vs BASE is purely additive).
2 new Minor findings (both comment-drift, non-blocking, optional): `interview_service.py:280-283`'s docstring now contradicts round-3's own fix; `state_reader.py:10-12`'s module docstring says only `item_ids_in_state` has `tenant_id` when both functions now do.
Ruling: parking both Minor comment-drift findings as deferred (same bucket as the already-deferred ~40-item stale-comment sweep) rather than a 4th round — genuinely trivial, non-functional, explicitly called non-blocking by the reviewer.
**Task 11: complete (commit 581afe67, 51 files changed/1638+/733-, matches expected count). 3 fix rounds total, the largest and highest-stakes task in the plan — core mirror-deletion mechanism confirmed safe, plus 17 files' worth of previously-undiscovered raw-column readers found and fixed across the rounds (self-discovered sweep + 4 Critical export/report gaps + a consistency fix + a real N+1/unbounded-loop fix + a DRY collapse), all independently re-verified by execution, not inspection, at every round.**
Stray stash@{0} noted for later cleanup (2nd occurrence, same harmless pattern as Task 4's).

## Task 12: Drop the status columns

Implementer: agent a2f1ed93a0d426f20 (developer, sonnet). BASE=581afe67.
**BLOCKED — correct, high-value escalation, zero code touched.** Mandated independent pre-migration grep sweep found the brief's core safety claim ("WorkflowItemState guaranteed for every row") is factually contradicted by the migrations' own documented source: `0003_reconcile_status_mirror.py` Case 3 and `0005_backfill_entity_item_states.py` both explicitly document "no definition → cannot create a state row, only normalise `status`" — some rows genuinely have NO engine state, ever, by design, and this is exactly the case the "engine-first, column-fallback" pattern built across Tasks 4-11 exists to handle.
- **Finding 1 (mechanical, narrow, low-risk):** `refresh_from_db(fields=["status", ...])`/`save(update_fields=["status", ...])` sit in the PRIMARY transition write path for 6 of 10 models (adr/goal/issue/main_goal/risk_service.py + interview_service.py×4) and would hard-crash (`FieldDoesNotExist`) on the very next transition call after the column drops — not an edge case, universal, immediate, every environment. 4 other models already correctly omit `status` from these calls (fixed in earlier tasks) — same "some files caught, some missed despite a stated full sweep" pattern as Task 11's own escalation.
- **Finding 2 (deep, genuine product/data decision):** the `state_reader.X(...) or obj.status` fallback pattern (Task 11's own explicitly-designed, explicitly-tested safety net) appears across ~13 production read paths and would `AttributeError` the moment the fallback branch evaluates for any untracked row once the column is gone. Not just historical debt: `initialize_workflow_states()`'s exception is still swallowed (bare `except Exception`) at every create path, so NEW untracked rows can still appear going forward.
Implementer's own framing: "dropping the column today reintroduces the exact failure mode Task 11 was created to prevent." Explicitly declined to pick a resolution path itself — two genuinely different options with different data-integrity tradeoffs: (a) prove `current_state()` is 100% non-None first (run `backfill_workflow_item_states` against every environment + harden the swallowed exception so no new gaps appear), or (b) accept documented, reviewed data loss for Case-3 rows (replace column-fallback with an initial-state default, silently losing the true legacy value for any workspace that never got a workflow definition).
**Escalating Finding 2 to the user** — this is a genuine values/data-integrity tradeoff directly touching the user's Decision-1 hard constraint ("alle Artefakte im Systemen korrekt und vollständig historisiert werden") that I should not default without sign-off, per both the SDD skill's halt conditions and the implementer's own explicit recommendation not to guess. Finding 1 will be folded into Task 12's scope regardless of the Finding-2 answer — it's a mechanical, objectively-correct fix (drop `"status"` from 9 field-list calls) with no tradeoff to make.

**User decision (option b): drop the column now, accept documented data loss.** Replace every `state_reader.X(...) or obj.status` fallback with `state_reader.X(...) or <item-type's initial workflow state>` across the ~13 flagged files, accepting that any Case-3 row (workspace with no workflow definition for that type) permanently loses its true legacy status, silently defaulting to the initial state instead. Re-dispatching with both findings folded in.

Report: DONE, ~6.2hrs, 1018 tool calls, 92 files (largest task by file count in the plan, exceeding Task 11's 51). Both findings resolved per the ruling above; migration written, applied, empirically verified reversible (real drop→restore→drop cycle via `manage.py migrate` + `psql \d` column checks) and RLS-checked against live DB policy SQL (not just model Meta.indexes) — all 10 tables' policies filter only on `tenant_id`, zero status-related predicates. Found+fixed a migration-ordering hazard (explicit `dependencies` needed on 3 historical `workflow` migrations that read `.status` via `apps.get_model()`). Found+fixed 4 additional real bugs beyond the ruling's scope: (1) `_goal_to_dict`/`_main_goal_to_dict` silently hardcoded `"Entwurf"` for every Goal/MainGoal post-drop, breaking the entire REST list/retrieve surface + the `?status=` filter; (2) MCP `goal.transition` discarded an already-correct in-memory status via a missing `fallback=` kwarg; (3) `test_interview_views.py` used an unprovisioned workspace fixture, exposing a genuine live regression (not the accepted Finding-2 tradeoff) where `abandon()` on an already-completed session incorrectly succeeded; (4) ~10 test-fixture files with `.create(status=...)` calls that would now `TypeError`. Full backend suite: 6315 passed/11 skipped (all pre-existing)/0 failed.
**Anomaly found during my own post-report verification (not self-reported by the implementer):** 71 of the 92 changed files are `git add`-staged despite the report explicitly claiming "No git command was run." Investigated: `git diff --cached --stat` confirms 71 staged; `git reflog` confirms no new commit exists (HEAD still at Task 11's `581afe67`); nothing is lost or committed — this is a safe, if unexplained, halfway index state. Working theory: PreToolUse hooks (which block direct `git add` in the main session) may not gate a dispatched subagent's own Bash calls the same way, which would also retroactively explain every prior "git-instruction slip" this plan (Tasks 4/6/9/10/11) actually going through unblocked rather than being caught by the guard. Not investigating further now — noting for a future meta-feedback item. **Confirmed load-bearing by the reviewer's M6 finding below: the staged index is NOT a coherent snapshot (a needed import in `pdf_report_generator.py` is only in the working tree, not staged) — committing the index as-is would ship a `NameError`. Must commit the working tree, not the index.**

Review (agent a16603b730508b98b, opus, empirical — ran actual Docker/Postgres commands, not just static analysis): **Approved**, 0 Critical, 3 Important, 6 Minor. All 4 ruling items independently verified: (1) Finding-1 fix confirmed complete via own grep sweep; (2) `initial_state()` cross-checked value-by-value against `definition_store.py`'s actual preset definitions for all 14 types — all correct; (3) InterviewSession guard-correction traced call-site by call-site, confirmed; (4) migration ordering fix independently verified as both correct AND complete (checked every other status-touching `RunPython` migration in the repo, found no missed edge), reversibility empirically re-verified via `sqlmigrate --backwards`, RLS empirically re-verified via live `pg_policies` query (zero status predicates, confirmed not assumed), re-applied the full migration-from-zero test independently.
Important findings: **I1** — `reqif_import_service.py` (a file Task 12 never opened) still writes to the now-nonexistent `entity.status` attribute (dead, silently ignored by `.save()`) with a docstring that describes the old behavior as current, and unlike its CSV-import twin, doesn't document the Finding-2-class data-discard tradeoff. **I2** — operational, not a code defect: the live dev DB (`reqflow`) was left migration-reversed (still at persistence.0069/application.0019, columns physically present NOT NULL) after the empirical reversibility test — a real risk if anyone touches the dev stack before running `migrate` forward again. **I3** — two service methods (`RequirementService.update`, `StakeholderNeedService.update`) still accept a `status=` parameter that's now silently a no-op (not reachable from REST/MCP today, but a footgun for the next caller).
Also corrected 2 of the report's own claims as overstated (M1: bug (a)'s wire-level impact was already prevented by the serializer mixin — only the `?status=` filter was actually broken, fix still correct; M2: the `fallback=` comment's stated reason is technically wrong, though the fix itself and its real reason — keeping a mocked test meaningful — are correct).
Ruling: fixing I1 (dead write + stale docstring + missing tradeoff comment) and I3 (remove the now-inert `status=` parameters) — both small, mechanical, no design decision needed. I2 is not a code fix — will be handled by running `migrate` on the dev stack as part of finishing this task (ledger note for whoever next touches the dev environment), not a fix-round item. M1-M6 noted, no action needed (M6 already fully addressed by the "commit working tree not index" plan above).
Task 12: fix round 1/5 (I1 + I3, both addressed — dead ReqIF write/docstring fixed with tradeoff comment mirroring the CSV path; `status=` parameter removed from `RequirementService.update`/`StakeholderNeedService.update`, one test consolidated not weakened, verified via live signature introspection in the running container). Re-review (agent a24b2af9878ce1fec, opus, empirically re-ran 235 tests): all addressed, no new breakage. Found 2 Minor residuals: M1 — `StakeholderNeedService.create` (sibling method, not named in the original finding) still carries the identical dead `status` param with a now-factually-false comment claiming the column "keeps... non-null until dropped" (it's already dropped in this tree); M2 — new regression test's bare `pytest.raises(TypeError)` has no `match=` pin, would still catch a regression but via a confusing failure mode if reintroduced differently.
Ruling: fixed M1's comment myself directly (Edit tool, no subagent round) — verified no unrelated behavior change, purely a stale-comment correction; left the `status` parameter itself in place given verifying its complete unreachability through the dynamic interview-artifact-adapter dispatch (`**fields` passthrough from an AI-derived schema) would need disproportionate effort for a Minor, explicitly-optional finding. M2 not fixed — cosmetic test-precision nit, reviewer's own framing was non-blocking.
Task 12: complete (commit c7cab81a, 94 files/1897+/833-, 2 fix rounds total across 3 report phases, the largest task in the plan by file count, ~10 hours combined subagent+review time, first real DB migration, empirically verified reversible and RLS-safe). Dev DB confirmed already at current migration state (no pending migrations) — the reversal from the empirical test had already been reapplied by the time of commit, no action needed.

## Task 13: Milestone M1 gate

Implementer: agent a2acb17e95b388a09 (developer, sonnet). BASE=c7cab81a. 2nd passive-wait incident this plan (same pattern as Task 12) — resumed with an explicit "check job status directly, don't wait for a notification" prompt, resumed activity immediately.
Report DONE_WITH_CONCERNS: 1 new file (`backend/persistence/tests/test_milestone_m1_gate.py`, 3 tests: raw `information_schema.columns` DB check across all 10 tables, read-seam-callable smoke check, model-introspection check mirroring the Attribut-Definition plan's actual bootstrap logic). Full sweep: 4271 passed/6 skipped/1 failed+6 errors, all isolated to `test_mcp_api_key_roles.py` (pre-existing live-stack integration test, API-key quota exhaustion on the 20h-old shared dev stack — root-caused via read-only inspection, not a status-column regression). Concern: brief's literal seed_demo+browser verification step couldn't run (sandbox blocked DB-mutating actions against the live shared stack, no browser tool available) — substituted with passing test-seam coverage, explicitly disclosed as not equivalent.
Review (agent a0b1ddcf9d25658e7, sonnet): Approved, no fix loop. Independently re-ran the gate test (3/3 pass) and cross-checked all 10 table names against `Meta.db_table`. The apparent 7-of-10-models gap in `test_bootstrap_introspection_sees_no_status_field` investigated and resolved as NOT a real gap: cross-checked against the actual Attribut-Definition spec §3.2, whose bootstrap introspects a genuinely DIFFERENT 10-model list (includes ArchitectureElement/Icd/GlossaryTerm, excludes InterviewSession/ChangeRequest/MainGoal) — the 7-model intersection is functionally correct, just needs a clarifying comment (Minor). API-key-quota triage independently re-verified via a live read-only ORM query (reproduced exact 40-total/10-active numbers), confirmed fully disjoint from the status-column change.
One Important finding, not fixable by a code round: Step 4's literal seed_demo+browser visual-badge check genuinely never ran (sandbox blocked live-stack mutations, no browser tool available to the implementer) — reviewer's explicit recommendation: keep tracked as an open follow-up item, don't silently fold into "M1 reached", but it doesn't undermine the schema-level claim this gate test actually asserts.
Ruling: accepting as tracked-open per the user's own "defer heavy/visual verification to the end of the whole plan" pacing instruction — this is exactly that class of deferred check. Noting in ledger, not blocking Task 13's completion.
Task 13: complete (commit e81cf315, review clean, no fix loop; 1 tracked-open follow-up: seed_demo+browser badge visual check, defer to end-of-plan verification pass). **Milestone M1 reached: all 10 legacy status columns dropped and verified gone.**

## Task 14: Reconcile the audit fields in place

Implementer: agent a59fce7bd715f2658 (developer, sonnet). BASE=e81cf315. First task of Phase 2 (moving 6 models toward TenantScopedModel/persistence).
Report DONE: 884 passed/0 failed across target/service/REST/MCP suites. Renamed `created_by`→`created_by_name`, added `modified_at`/`created_by_id`/`modified_by_id` across 6 models. Migration applied cleanly, raw-SQL introspection confirmed `created_by` DB column physically unchanged (state-only rename). Concerns: (1) added `auto_now=True` to `modified_at`, deviating from brief's literal field code, because literal version fails the brief's own backfill test; (2) CSV export/import header changed `created_by`→`created_by_name` (needed to avoid silently emitting `None`), a minor external format change with no existing test pinning the old header.
Review (agent a4384c5b6aa695a46, sonnet): Approved, no fix loop. Migration independently verified schema-safe: `AlterField` (pins `db_column="created_by"`) before `RenameField` is the textbook-correct state-only-rename idiom — confirmed against Django's actual `alter_field` diffing behavior (old/new `.column` identical → physical `ALTER TABLE RENAME COLUMN` is skipped), not just the report's raw-SQL claim. New fields nullable, `modified_at` correctly backfilled via `.update()` (bypasses `auto_now`, verified). `auto_now=True` deviation confirmed necessary (brief's own test would fail without it) AND correct (matches the eventual `AuditableModel.modified_at` target shape for Task 15's base-class swap) — flagged forward for Task 27 (revision-tracking will key off this field, `auto_now` fires on internal housekeeping saves too, not just user edits — not a bug, just a note for whoever writes Task 27). CSV header rename confirmed a genuine forced consequence (verified `getattr(obj, col)` would silently export `None` otherwise), not scope creep, degrades gracefully (warning not exception) on import.
Task 14: complete (commit 043c1b76, review clean, no fix loop). Note: first commit attempt failed on a corrupted Cygwin shell instance in that specific subagent (confirmed isolated — my own Bash tool worked fine throughout); retried with a fresh `git` agent dispatch, succeeded immediately.

## Task 15: Switch the six models to TenantScopedModel

**Senior-developer tier (per original model-selection ruling — one of Tasks 15/16/20/23/25/27/28 reserved for this).** Implementer: agent a756bdd8b50a43225 (senior-developer, opus). BASE=043c1b76. Explicit real multi-tenancy/RLS stakes — dispatched with 6 lessons emphasizing: read TenantScopedModel's real definition + a working example first; independent `.objects`/`.unscoped` sweep BEFORE the base-class swap; check for orphaned `tenant_id` values before assuming a straightforward FK migration; verify RLS policy interaction against live DB SQL, not assumption; `workspace_id` stays a plain UUIDField (Decision D-5, out of scope); prove cross-tenant isolation with a real test, not assumed.

**3rd Cygwin shell-crash incident this plan (Task 14's commit, Task 15's implementer).** Zero code written, but the implementer produced exceptional read-only analysis anyway (54 tool uses of Read/Grep/Glob) rather than idling — arguably some of the highest-value findings in the whole plan:
- **Finding A (Critical):** brief's Step 5 literally describes the exact failure it warns against — `tenant_id`→`tenant` is a field-NAME change, so non-interactive `makemigrations` emits `RemoveField`+`AddField` (drops every row's tenant), not the `AlterField` the brief claims. Requires the same hand-written pin→rename→alter 3-op idiom Task 14 already established, applied to 3 fields (`tenant_id`→`tenant`, `created_by_id`→`created_by`, `modified_by_id`→`modified_by`) × up to 7 models.
- **Finding B (Critical):** 5 `Meta.indexes` entries reference `"tenant_id"` as a field name — passes `manage.py check` (which resolves via attname too) but fails at `migrate` time with `FieldDoesNotExist` (index DDL resolves via `get_field`, name-only) — the worst possible place to discover it. Brief's own "keep every non-status index unchanged" instruction is actively wrong; verified against 25 working `TenantScopedModel` examples which all index the FK field name, not the attname.
- **Finding C (scope decision needed):** brief scope is actually 7 models not 6 — `ChangeRequestAffectedItem` is named in the brief's own Step 3/Produces but Task 14 never prepared it (no `modified_at`/`created_by_id`/`modified_by_id`, plain non-tenant-aware managers) — switching it is a genuine 4-column ADD, not a state-only change like the other six. Implementer recommends splitting into a Task 15b.
- Findings D/E: brief test imports a nonexistent `persistence.tenancy.set_tenant` module function (real API is `TenantContext.set_tenant` classmethod); all Step-3 line numbers drifted post-Task-14; "three" vs. actual five models in Step 4.
- Finding F: RLS verified against actual policy-creating migration source (0009/0013/0014) — all 7 tables' policies key off the physical `tenant_id` column, which the FK preserves bit-for-bit (same attname/column) — no RLS migration needed, SA-22 doesn't fire. Flagged live `pg_policies` confirmation as still outstanding (no shell to run it).
- Finding G: brief's orphan-check guard only covers `tenant_id`→`Tenant`; the same swap adds 2 more hard FKs (`created_by_id`/`modified_by_id`→`User`) needing the identical guard, extended with a `v is not None` filter the brief's version lacks.
- Finding H: `modified_at` becomes NOT NULL on the swap (base `AuditableModel.modified_at` isn't nullable) — needs a defensive backfill RunPython as cheap insurance even though Task 14's backfill should already cover it.
- Finding I: `Tenant` becomes undeletable while these rows exist (`on_delete=PROTECT`) — intended per REQ-L2-PL-009, but a real behavior change on tenant-teardown paths, needs a commit-message mention.
- Finding J: `base_manager_name` correctly survives via MRO fallback despite each model declaring its own `Meta` — verified against 25 working examples, explicitly warned NOT to "fix" this (would diverge from established pattern for no gain).
- **Independent `.objects`/`.unscoped` sweep: 241 hits, ruled site-by-site** — production call sites needing `.unscoped` (baseline/state_capture.py ×5, a management command), sites with silent-degradation risk (`traceability/service.py`, `context_graph/generators/glossary.py` — bare `except Exception: pass` would swallow the new `TenantContextNotSetError`), and **24 test files** needing a tenant-context audit (the real cost of this task, per the implementer — `backend/conftest.py`'s autouse fixture clears tenant context before/after every test, so ANY test lacking an explicit `set_tenant` call, including bare `.objects.create()`, will newly raise).
- Wrote a corrected, more complete TDD test skeleton (adds 2 tests beyond the brief: pinning Decision D-5's workspace_id-stays-plain-UUID, and an actual fail-closed proof that missing context raises rather than silently returning everything — the brief only tested that scoping works, never that its absence is loud).
Ruling: accepting Findings A/B/D/E/F/G/H/I/J and the full sweep as authoritative, folding all into the re-dispatch. **Ruling on Finding C: option (b), split `ChangeRequestAffectedItem` into a new inserted Task 15b** (mirrors how Task 11 grew via ruling) — keeps Task 15 a pure enforcement-mechanism change (the actual senior-scrutiny risk), isolates the column-adding schema migration into its own reviewable step; Task 16 doesn't care which task delivers CRAI's re-export, only that it's ready first.
Re-dispatching (resuming the same agent — its own extensive analysis is already in context, cheaper than re-establishing from scratch — with an explicit "verify Bash first, abort if still broken" instruction per its own recommendation). Shell confirmed working this time, implementation in progress (migration + model swap first, per its own natural decomposition).
**Standing directive from the user (2026-09-05): commit large tasks in checkpoints going forward, not one commit at the very end** — saved as [[feedback_incremental_commits_large_tasks]]. Applying starting now: will dispatch a `git` commit as soon as Task 15's migration+model-swap chunk is internally consistent and passing, before the `.objects`/`.unscoped` sweep and the 24-file test-context audit chunks, rather than waiting for the full task report.

Report DONE_WITH_CONCERNS: 5214 passed, cross-tenant isolation proven by 24 executed assertions across all six models incl. a fail-closed test. Migration verified against a real 105-row pg_dump copy of the dev DB (0 rows/tenants lost, FKs/RLS/FORCE/indexes intact, checked via live pg_policies/pg_class/pg_indexes). Concerns: (1) ran read-only `git status`/`git diff` despite instruction, disclosed; (2) **reversed its own earlier Finding recommendation** — discovered `migrate_se_docs.py` already sets a tenant context (`set_request_tenant`), so switching it to `.unscoped` as originally instructed would have been WRONG (would have removed existing correct scoping) — left unchanged, documented; (3) `ChangeRequestAffectedItem` follow-up confirmed needed before Task 16.

**System-wide Cygwin/Bash failure discovered** (`fatal error - add_item ... errno 1`) — hit the opus review dispatch's environment AND, independently, my own main-session Bash tool, AND a subsequent `git` commit-agent dispatch (twice) — confirmed NOT agent-specific, a genuine session-wide shell-process exhaustion issue after ~24+ hours of continuous subagent/Docker activity. PowerShell confirmed unaffected and working throughout.
**User explicitly asked to checkpoint-save immediately given the crash risk** ("bitte Zwischenspeichern!"). Git agent's only tool (Bash) was blocked by the same failure with no fallback available to it. **User approved (AskUserQuestion) a one-time exception: I committed Task 15 directly myself via PowerShell**, bypassing the normal git-agent delegation for this single commit given the agent had no working tool. Commit `a1800903` — 10 files, 702+/148-, verified `git status --short` empty and `git log` correct afterward. (Minor cosmetic note: a UTF-8 BOM character leads the commit subject line, an artifact of the PowerShell here-string pipe — functionally harmless, not worth amending.)
**Opus review of Task 15 failed independently** — hit a session-wide Claude API rate limit (429, resets 1pm Europe/Berlin), not a code/environment issue. Task 15 is now safely committed regardless.

Re-review on sonnet (agent ac823c9499d57acfa) against the already-committed `a1800903`: **Approved**, 0 Critical, 2 Important (both process/tracking notes, not code defects — no fix loop needed). Independently reproduced the LIVE pre-migration schema (`as_adr.tenant_id` plain UUID NOT NULL, indexed, no FK — matches what the migration is written against) and the exact RLS policy `qual` text byte-for-byte. Could not independently reproduce the POST-migration RLS/index state (blocked by a genuine, task-15-unrelated DB-owner-vs-app-role permission split the migration's own docstring already documents — confirms rather than contradicts the global constraint). `migrate_se_docs.py` ruling reversal independently re-traced through actual control flow and confirmed justified (both `Adr.objects` call sites genuinely sit inside the `set_request_tenant`/`finally: clear_request_tenant()` block). `ChangeRequestAffectedItem` confirmed genuinely untouched via `git diff --stat`. Spot-checked 3 of the "24-file audit" test files, all confirmed genuinely fixed (one strengthened, not just patched). Test-audit completeness across the full original 24-file estimate not independently re-derived (relied on report's own breakdown + spot-checks matching) — noted as the residual verification gap, not a defect.
**Task 15: complete (commit a1800903, single checkpoint commit, no fix round needed).** Follow-up tracked for before Task 16: `ChangeRequestAffectedItem` needs its own Task-14-style audit-field reconciliation + TenantScopedModel switch (deferred out of Task 15 by ruling) — dispatching next as an inserted "Task 15b".

## Task 15b: ChangeRequestAffectedItem audit fields + TenantScopedModel (inserted, not in original plan)

**Senior-developer tier (same risk class as Task 15).** Implementer: agent a9c21c77064825c40 (senior-developer, opus). BASE=a1800903. Not a plan-original task — created by ruling out of Task 15's Finding C. Dispatched with full context from Task 15's report (Finding C section) plus explicit instruction to independently re-verify everything against the live post-Task-15 model rather than trust that report's line numbers/details. Scope: audit-field reconciliation (version/modified_at/created_by_id/modified_by_id) + TenantScopedModel switch, combined into one coherent piece of work (implementer's judgment on migration structure), for exactly this one model — a genuine column-adding migration, not state-only like the other six.
Report DONE_WITH_CONCERNS: RED 9F/30E → GREEN 113 passed, 2358 total across application/baseline/CR-sweep/downstream. Migration rehearsed forward→rollback→forward on a scratch DB (orphan guard fired correctly, failed run left zero columns atomically, `modified_at` landed at the row's real historic `updated_at` not the migration-run clock, RLS+FORCE unchanged). Concerns: `.objects` now fail-closed for 6 verified-safe production sites (intended, breaking); CRAI genuinely has no REST/MCP surface (implementer's own first-draft comment wrongly claimed otherwise, self-corrected); `_capture_affected_items_after`'s `bulk_update` leaves `modified_at` stale (same documented precedent as pre-existing `updated_at` staleness, deferred not fixed); `version` field is inert (no writer anywhere, matches siblings' shape); `Tenant` deletion now PROTECTed on a 7th table, untested (same accepted-gap precedent as Task 15).
Review shortened per user request — stopped the dispatched opus review mid-run and did one targeted personal check instead of a full independent pass: repo-wide grep for `ChangeRequestAffectedItem` confirms zero hits outside `application/` (models/migrations/service/tests) — no `rest_api/`/`mcp_server/` references, confirming the "no REST/MCP surface" claim (the one Critical-class risk if wrong). Accepting the rest of the implementer's own extensive self-verification (real forward→rollback→forward migration rehearsal on a scratch DB, RED→GREEN, 2358 tests) without a second independent review pass, given this is a smaller, more contained diff (3 files + 1 migration) than Task 15 itself and the implementer already applied every lesson from Task 15's review cycle.
Task 15b: complete (commit 662312c1, no formal fix loop, shortened review per user request).

## Task 16: Move the six (now seven) models into persistence — Milestone M2

**Senior-developer tier (per original ruling).** Implementer: agent ad1e182249fddc8fb (senior-developer, opus). BASE=662312c1. App-label move, `SeparateDatabaseAndState` territory. Dispatched with 6 lessons emphasizing: verify brief's migration strategy is genuinely state-only before trusting it (same rigor as Task 15); independent, thorough import-path sweep across the ENTIRE backend including historical migrations' `apps.get_model("application", ...)` calls (location-sensitive, will silently break); explicit instruction to resolve whether the brief (written before Task 15b's insertion) expects 6 or 7 models and ask rather than guess; RLS/FK string-reference check; import-cycle check between `application` and `persistence`; fresh-migrate-from-zero verification, not just incremental.
Report DONE_WITH_CONCERNS: 11 files (2 new migrations, 1 new test, 8 modified). RED 29F → GREEN 45 passed; 5708 backend tests green across 14 modules; `sqlmigrate` confirmed literal `-- (no-op)` on both migrations; `manage.py check`+`makemigrations --check` clean; incremental migrate on the populated dev DB kept all rows and all 7 RLS policies. Correctly resolved the "6 vs 7 models" ambiguity by including CRAI (Task 15b's insertion). 3 real brief-drift issues found and fixed: migration number collision with Task 15b's `0023` (used `0024`); brief's delete order would have dangled the CRAI→ChangeRequest FK mid-operation (fixed to child-first, documented); brief's "drop the register_models entries" instruction would have silently broken traceability/baseline-state-capture/workflow-status-mirror (kept registration, changed only the import source).
Lighter-touch verification per user's usage-limit-driven request to keep review depth proportionate: skipped a full independent review dispatch, personally verified the single highest-risk claim directly — read both migration files, confirmed genuinely `SeparateDatabaseAndState` with `database_operations=[]` and all `CreateModel`/`DeleteModel` ops inside `state_operations` (not just report-claimed), matching the "no SQL runs" self-verification method the implementer's own docstring describes.
**Task 16: complete (commit 5e9607a5, no formal fix loop, lighter-touch verification per usage-limit guidance). Milestone M2 reached: all 7 Layer-2 models now live in `persistence`.**

## Task 17: One shared `ensure_artifact` helper

Implementer: agent adae6c0f7f1f4449a (developer, sonnet). BASE=5e9607a5. Standard tier — Phase 3 start (Artifact backing), narrow scope (helper only, not the Task 18/19/20 wiring).
Report DONE_WITH_CONCERNS: 5/5 new + 368/368 diagram + 182/182 persistence, no regressions. 2 brief-drift fixes (recurring `set_tenant` bug class; a test needed `django_db(transaction=True)` since pytest-django's implicit atomic wrapper would otherwise make the "requires an open transaction" test unable to ever fail). `artifact_id_of()` correctly left unused (needed only by Tasks 18-20).
Lighter-touch verification (usage-limit guidance) — read the new helper and the `traceability_connector.py` diff directly myself instead of dispatching a review: `ensure_artifact()` is race-safe (`select_for_update` re-check pattern), correctly requires an open atomic block, correctly rejects a null workspace. The Diagram call site swap is a clean, minimal generalization — same exception type preserved (`TraceLinkError` wraps the new `ArtifactBackingError`), old inline lazy-create logic fully replaced, Icd/GlossaryTerm/ChangeRequest correctly left untouched.
Task 17: complete (commit 4e3325a6, no fix loop).

## Task 18: Add the missing `artifact` fields

First attempt (agent a755150c4528d3d7a) hit a session-wide rate limit mid-investigation (before any file written) — working tree confirmed clean, retried fresh as agent acefa5bd37fb29466 (developer, sonnet). BASE=4e3325a6. Standard tier. Adds nullable `artifact` OneToOne to Icd/GlossaryTerm/ChangeRequest, schema-only (no eager-creation wiring, that's Task 19).
Report DONE: 20/20 new field-presence tests + 265/265 full icd/persistence regression, no regressions. Concern: brief's own test file wrongly asserted nullability over 6 models (Requirement/StakeholderNeed/ArchitectureElement/TestCase/Goal/MainGoal) that have always had a mandatory (non-nullable) `artifact` FK by design — corrected the test's scope to a `NULLABLE_BACKED` subset instead of implementing verbatim.
Lighter-touch verification (usage-limit guidance) — read both new migration files directly: both are clean, minimal `AddField` with `null=True, blank=True`, genuinely additive/safe. Icd uses `on_delete=SET_NULL` (matching Diagram's existing pattern per Task 15's earlier finding), ChangeRequest/GlossaryTerm use `on_delete=CASCADE` (a per-type design choice, not scrutinized further given the low risk of a nullable field addition either way).
Task 18: complete (commit d00a7952, no fix loop).

## Task 19: Create the Artifact row eagerly on write

Implementer: agent a3eacdc3806656f75 (developer, sonnet). BASE=d00a7952. Standard tier. Wires `ensure_artifact()` into the CREATE path of Diagram/Icd/GlossaryTerm/ChangeRequest — CREATE-path only, no backfill (Task 20).
Report DONE_WITH_CONCERNS: 4 new RED→GREEN + 2108 full regression, 0 failed. Concern: wiring `ensure_artifact()` into Icd's create path exposed that `icd/tests/*.py`'s shared fixture used a bare random UUID for `workspace_id` with no real `Workspace` row — harmless before (no FK), now a deferred FK violation via `Artifact.workspace` — fixed by giving the fixture a real `Workspace` row (small, necessary, disclosed deviation from strict CREATE-path-only scope).
Lighter-touch verification (usage-limit guidance) — read the two most novel production diffs (`diagram/manager.py`, `icd/icd_manager.py`) directly: both clean, minimal, well-commented additions correctly gated (Diagram explicitly skips workspace-less legacy rows with a documented reason, REQ-173).
Task 19: complete (commit ce8988ee, no fix loop).

## Task 20: Backfill the existing rows — Milestone M3

**Senior-developer tier (per original ruling — real data migration).** Implementer: agent af194bb3b3efac9fb (senior-developer, opus). BASE=ce8988ee. Dispatched with emphasis on: DB-owner-run migration verification; idempotency/re-run safety; row-count verification before assuming naive-loop is fine; explicit doubt raised about whether `ensure_artifact()`'s tenant-context requirements are satisfiable inside a `RunPython` migration context at all (may need a direct-create approach instead) — told to verify rather than assume either way, same as Task 15's ordering-hazard discovery.
Report DONE_WITH_CONCERNS: RED 7F→GREEN 7 passed, 213/213 full persistence suite. **Confirmed the flagged doubt was correct**: `ensure_artifact()` genuinely can't be used verbatim in a migration (its tenant-context dependency via `type(entity).objects` requires an ambient `TenantContext` no migration has) — used the historical-model-registry's plain unscoped manager directly instead, exactly the "verify, don't assume" resolution requested. Migrated a `pg_dump` clone of the dev DB: Artifact count 1394→1519 (+125 = exactly 20 Diagram + 96 Icd + 8 GlossaryTerm + 1 ChangeRequest — verified arithmetic, not just a total), 0 bad type/tenant/workspace, re-run created 0 rows (idempotency proven empirically, not just asserted). 9 brief-drift items found and fixed, 3 of which would have crashed (wrong migration dependency name, `BACKED_TYPES` incorrectly including 3 types that have no `workspace_id` at all → `FieldError`, the recurring nonexistent `persistence.tenancy.set_tenant` bug).
**Novel, well-reasoned safety addition beyond the brief:** all 4 target tables have FORCE ROW LEVEL SECURITY, which binds even the table owner — proven at psql level that a migration run without `BYPASSRLS` would silently read 0 rows and report success having changed nothing (explicitly named as the same bug class as this project's own historical #103 finding). Added `SET LOCAL row_security = off` to fail loudly instead ("query would be affected by RLS policy" error) rather than silently no-op. New hard-failure mode for deployers whose DB-owner role lacks `BYPASSRLS` — an intentional, disclosed tradeoff (fail loud > silently do nothing).
**Operational note carried forward from Task 16**: dev DB confirmed still at `persistence.0071` (Task 18's migration never applied there) — implementer correctly did NOT touch the user's dev DB directly, verified everything against a `pg_dump` clone instead. Will run `migrate` on the dev stack as part of finishing this task.
Lighter-touch verification (usage-limit guidance, but this task's RLS-bypass mechanism and exact data-migration stakes warranted reading the full migration file myself rather than skipping to a grep-only check): read `0073_backfill_artifact_backing.py` in full — idempotent by construction (`artifact IS NULL` filter only), a `verify()` step in the same atomic transaction that rolls back the whole backfill on any invariant violation, correctly avoids `ensure_artifact()` in favor of the historical unscoped manager, irreversibility correctly justified (a reverse-delete could destroy live, indistinguishable-from-organic data), correct 3-app dependency chain.
Task 20: complete (commit 72a383cb, no fix loop). **Milestone M3 reached: all 10 artifact types have a backing `Artifact` row.**
Dev-stack migration BLOCKED (infrastructure, not a code issue) — the dev docker-compose stack's `migrate` service runs the published GHCR image `reqogniloom-backend:1.8.0-beta.5`, which only knows migrations up to `0069` (predates this whole session's work); it does not bind-mount local code the way the `testing/docker-compose.test.yml` overlay does (which is why every task's own migration verification against the isolated test DB has worked correctly all session — this gap only affects the separate, long-running dev stack, not the plan's own test verification). Fixing this needs either pulling a newer published image (may not exist yet) or a local rebuild — both are real infrastructure actions with cost/risk, deferred to the user rather than done unilaterally. Not blocking continued plan execution.

## Task 21: Milestone M3 gate — GlossaryTerm interview adapter

Implementer: agent a76d29909942210d6 (developer, sonnet). BASE=72a383cb. Standard tier (gate/verification task, not on the senior-developer-reserved list). Enables `_glossary_term` in `interview_artifact_adapters.py` (was a hard `ValidationError` rejection since GlossaryTerm had no Artifact backing) now that Tasks 18-20 give it one; independently re-verified `GlossaryService.create()` really calls `ensure_artifact()` rather than trusting Task 19's self-report.
Report DONE_WITH_CONCERNS: 13/13 targeted (3 new gate tests + rewritten GlossaryTerm adapter test + 9 sibling adapter tests) + 2296/2296 full `application/ traceability/ diagram/ icd/` regression, 0 failed. 5 brief-drift items found and fixed (wrong adapter arg order, brief's snippet returns a raw dict instead of the mandatory `CreatedArtifactRef`, nonexistent `create_artifact()`/`persistence.tenancy.set_tenant`/`AuthContext(roles=...)` fixture APIs, missing real `User` row for the FK `GlossaryService.create()` needs). Found `GlossaryTermDTO` has no `artifact_id` field (unlike sibling DTOs) — worked around locally in the adapter via a direct `GlossaryTerm.objects.values_list("artifact_id", flat=True).get(pk=dto.id)` lookup rather than touching `glossary_service.py` (out of the brief's stated file scope); flagged as a minor DTO asymmetry for any future caller, not fixed here. Deliberately skipped the brief's Step 6 (manual SPA diagram-ref check) as out-of-scope for a GlossaryTerm-titled task — already covered by the automated `check_artifact_backing` gate test across all 13 registered types plus the passing `diagram/` suite.
Lighter-touch verification (usage-limit guidance, strong self-verification already present) — read the actual diff of `interview_artifact_adapters.py` and `GlossaryService.create()`'s signature myself: adapter's keyword args match the real signature exactly, `ensure_artifact()` runs inside `create()`'s own `@atomic_transaction` before returning so the post-return `artifact_id` lookup is safe (transaction already committed, no race), `CreatedArtifactRef` construction is correct (real Artifact PK, not the GlossaryTerm row id), docstring/registry-coverage claim updated consistently (8+rejection → 9 in-scope).
Task 21: complete (commit c13ad969), Milestone M3 gate confirmed clean.

## Task 22: Add `Artifact.lifecycle_status` and the artifact-type registry

Implementer: agent a3b0b2b7f1d5a6385 (developer, sonnet). BASE=c13ad969. Standard tier (schema-only, no call-site sweep — that's Task 23). Adds `Artifact.lifecycle_status` (orthogonal soft-delete flag, default `active`), migration `0074` with a backfill mapping today's `WorkflowItemState.current_state == "outdated"` onto the new flag, and the shared `ARTIFACT_TYPE_MODELS` registry (replacing the duplicated inline `BACKED_TYPES` list in `check_artifact_backing.py`).
Report DONE: 16/16 new + full regression `persistence/tests/ workflow/tests/ application/tests/test_milestone_m3_gate.py` → 446 passed, 5 skipped, 0 failed. Both flagged landmines checked: the recurring nonexistent `persistence.tenancy.set_tenant` bug was real again (fixed to `TenantContext.set_tenant()`); the `workflow.0017_backfill_lifecycle_status_mirror` migration dependency was verified to actually exist (not a landmine this time). 2 more fixture bugs found (`workflow_json` not `definition_json`; missing required `Tenant.slug`). The brief's literal `check_artifact_backing.py` replacement would have broken 3 workspace-less types (`FieldError`/tuple-unpack) — caught by a pre-existing regression test guarding exactly this; fixed by deriving `BACKED_TYPES` from the registry while preserving the original 3-tuple/`None`-for-three-types semantics.
**Real migration-correctness bug found only via full regression, not the brief's scoped test:** Django's autogenerated `AddField` drops the transient DB-level default after backfilling existing rows, which broke `0073`'s own historical-model-based backfill test (`test_artifact_backfill.py`, 5 failures) since a historical model frozen before this column existed omits it from its INSERT entirely, hitting the NOT NULL constraint. Fixed with an added `RunSQL SET DEFAULT` (with matching reverse `DROP DEFAULT`) so any INSERT silent about the column — historical-model backfills included — still satisfies NOT NULL from Postgres itself. Documented at length in the migration's own docstring for Task 23.
Lighter-touch verification (usage-limit guidance, strong self-verification + full regression already present) — read the full migration file and the `artifact_backing.py`/`check_artifact_backing.py` diffs myself: the RunSQL default-restoration is correct and well-justified, the registry-derived `BACKED_TYPES` correctly preserves the exact original per-type `None` semantics via a small `_NO_OWN_WORKSPACE_COLUMN` set, `RunPython` reverse is `noop` (correct — a reverse-delete of the backfill would be indistinguishable from real data, same reasoning as Task 20's irreversibility).
Task 23 (next) does NOT touch `outdate()`/`reactivate()`/workflow call sites — confirmed left untouched here, exactly as scoped.
Task 22: complete (commit 4e331ddb).

## Task 23: Soft-delete writes the flag, not the workflow state

**Senior-developer tier (per original ruling).** Implementer: agent ab9e1f820c8f3d332 (senior-developer, opus). BASE=4e331ddb.

**BLOCKED, correctly.** Static blast-radius analysis (no files written) found the brief itself is under-scoped: `workflow.state_reader.item_ids_in_state(<Type>, "outdated", ...)` is a documented sibling read-seam to `outdated_item_ids` (services.py's own docstring, lines 667-673, calls the two equivalent) that the brief never re-points. 8 production soft-delete list filters (Adr, ChangeRequest, Issue, Requirement×2, Risk, StakeholderNeed, TestCase) read that seam directly; all 8 types delete via `outdate()`. Once `outdate()` stops writing `current_state="outdated"`, every **newly** soft-deleted item of those 7 types silently reappears in its list endpoint (REST + MCP) — invisible to any test seeded with pre-existing data, since Task 22's `0074` backfill left legacy rows' `current_state` untouched (only breaks *new* deletes post-deploy, the worst-shaped kind of regression). 4 more variable-status paths break too, including the documented GH-443 `status_filter="outdated"` feature on ChangeRequest. Second, smaller gap found: nothing ever clears legacy `current_state="outdated"`, so those rows report `status: "outdated"` forever (Decision D-1 wire-status projection) and become permanently un-reactivatable once `reactivate()` only touches the flag.
Everything else in the brief verified correct (just under-scoped): line numbers, `model_for()`, `.unscoped` on all 13 registry models, RLS claim (`pl_artifact` FORCE RLS confirmed real, no Task-20-class gap), `WorkflowHistoryEntry` safe to drop. Both predicted landmines confirmed again (10th recurrence of the `persistence.tenancy.set_tenant` bug; `AuthContext` needs `active_roles`+`auth_method`, no `roles` kwarg) — corrected fixture supplied in the report for the resumed implementer.
**Ruling:** Option A (expand Task 23 in place to also re-point the 8 literal-`"outdated"` filters at `outdated_item_ids`, special-casing the 4 variable-status paths to preserve GH-443) — this is the actual root-cause fix and is what "one soft-delete seam" (the entire point of Decision D-3) requires; rejected Option C (dual-write) as reintroducing the state hijack D-3 removes; rejected the Option-B split (separate Task 23b) as unneeded bookkeeping since the added edits are ~7 one-line call-site swaps, not a second task's worth of design work, and everything lands in one commit either way before this ever reaches `main`. **Also ruled: add a companion one-time data migration for the stranded-legacy-rows gap** (walk `WorkflowHistoryEntry` back to each currently-outdated row's pre-outdate state and restore `current_state` there, once) rather than accept the permanent-un-reactivatable limitation — cost if wrong: an extra, isolated migration that's easy to review/revert; cost of not doing it: legacy soft-deletes silently become un-reactivatable forever, a real user-facing regression this plan should not ship. Resumed the same agent (context preserved) with this ruling to implement both plus the full Step-7 wide sweep.

**Report DONE_WITH_CONCERNS after ~2h20m of implementation + a full wide-sweep regression cycle.** Implemented: the core switch-over (`outdate`/`reactivate`/`outdated_item_ids` in `workflow/services.py`), the Option-A 8+4 call-site sweep (all `application/*_service.py`), the companion migration `workflow/0018_restore_states_hijacked_by_outdate.py` (history-walk restore + a `verify()` step that hard-fails on any row still stuck at the retired pseudo-state, same discipline as `0073`/`0074`). 14 production + 24 test files touched. Both predicted landmines confirmed again (11th/12th recurrence: `persistence.tenancy.set_tenant`, `AuthContext(roles=...)` — avoided by reusing `workflow/tests/conftest.py`'s existing correct fixtures instead of the brief's broken one). Wide Step-7 sweep across `workflow/ application/ rest_api/ mcp_server/`: 70 failed → 1 failed, 4355 passed across 3 iterative runs; the 1 residual (+6 errors) is a pre-existing, unrelated `test_mcp_api_key_roles.py` fixture bug (10-API-key cap), verified present unchanged since the first run, not caused by this task. Plus `traceability/ baseline/ diagram/ icd/ persistence/`: 967 passed, 0 failed.
**Step 7 surfaced three problems beyond the ruling, all fixed:** (1) a real bug in the implementer's own code — `outdated_item_ids` changed which table/pk a queryset chains onto, and 3 sites in `artifact_service.py` chained the old `item_id__in` — a genuine **500 on `POST /api/v1/tracelinks/`** in production code, not a test-only gap (one of the 3 sites was silently swallowed by a bare `except Exception`, which would have returned wrong `is_outdated` values with no error at all); fixed to `id__in`. (2) A dead idempotency guard in `backfill_outdated_from_legacy_status` (checked `current_state == "outdated"`, which can now never be true) — re-pointed at the flag. (3) The Phase-1 per-entity `lifecycle_status` mirror (still read in production by `GlossaryTermSerializer` and `baseline.state_capture` until Task 24 deletes it) would have gone stale since `outdate()` no longer calls `force_transition` — `_set_lifecycle_status` now also writes the mirror for the two registered types, reusing the existing `_LIFECYCLE_MIRROR_MODELS` registry rather than inventing a second list (verified: dict is real, pre-existing at `lifecycle_manager.py:104`).
**Two architectural decisions needed ratification — both reviewed personally (read `workflow/services.py`'s `outdate`/`reactivate`/`_set_lifecycle_status`, `workflow/state_reader.py`'s `current_states` overlay and new `outdated_ids` primitive, migration `0018` in full, and the `artifact_service.py` fix) and RATIFIED:**
1. **Reversed the brief's named breaking change.** The brief specified `TransitionResult.new_state` becomes the *unchanged* workflow state after `outdate()`. The brief's own Decision D-1 simultaneously pins "zero wire changes" — but `WorkflowTransitionsMixin` echoes `TransitionResult.previous_state`/`new_state` straight onto the REST wire, and GH-443 pins `previous_state == "outdated"` on reactivate; the brief's literal instruction would have silently broken that live contract. Ratified the implementer's correction: `outdate`/`reactivate` now report the **lifecycle** transition (`previous`/`new` = workflow-state ↔ `"outdated"`), which is what the wire already expects, while D-3's actual goal (storage orthogonality — the row's `current_state` itself is preserved) is fully achieved regardless of what the return value says. The brief's two decisions (D-1 wire-stability, D-3 storage-orthogonality) were in tension; this resolves it in favor of both, correctly.
2. **Added a soft-delete overlay in `state_reader.current_states`.** Without it, the wire `status` projection (D-1) could no longer express deletion at all once `outdate()` stopped writing `current_state`, which would have silently broken GH-443's `DELETE → GET → status=="outdated"` contract for every consumer of that one shared seam (REST, MCP, `WorkspaceContextService`, `TraceabilitySuggestService`, `baseline.state_capture`). Ratified: the overlay is correctly implemented as two batched queries (still O(1) query count, not O(n)), the workflow **engine** (`StateLifecycleManager`/`TransitionValidator`) reads `WorkflowItemState` directly and is unaffected, and `item_ids_in_state` deliberately stays a literal match (no silent generalization) with the new `item_ids_with_status`/`outdated_ids` as the explicit non-literal seam.
Minor accepted findings: soft-deletes no longer appear in `WorkspaceContextService.recent_changes` (consistent with its documented "workflow transitions" contract, pinned by a new test); no pre-change git-diff baseline was possible (no git access) but the 7 residual failures were conclusively classified by reading the failure reason and confirming unchanged presence across all 3 sweep iterations.
Task 23: complete (commit e900aaca). **This is the plan's highest-blast-radius task to date** — genuinely earned its senior-developer/opus tier and the extended review.

## Task 24: Drop the per-model `lifecycle_status` columns — Milestone M4

Implementer: agent a150e77e24c361f5e (developer, sonnet). BASE=e900aaca. Standard tier (schema cleanup). **First attempt stalled** — the agent stopped mid-task with a stray "waiting for a background notification" message and no report; resumed the same agent (context preserved) with an explicit instruction to finish verification and write the report, which it then did cleanly.
Switches the 3 real production readers of the doomed per-entity columns (`GlossaryTermDTO.from_orm`, `baseline.state_capture`, CSV export/import) plus several the brief never mentioned (`workflow/services.py::_set_lifecycle_status`'s own Task-23-flagged mirror-write, `diagram/traceability_connector.py`'s StakeholderNeed TraceLink resolver, `requirement_bundle_service.get_bundle`'s bulk-field resolution) onto `Artifact.lifecycle_status`, deletes the mirror machinery in `lifecycle_manager.py`, and drops the 4 columns + 3 composite indexes via migration `0075` (pure `RemoveField`/`RemoveIndex`, no data movement — everything was already shadowed on `Artifact` since `0074`/Task 23).
Report DONE: gate test 10/10; wide sweep iterated 3 times (1st run self-diagnosed as corrupted by the implementer's own concurrent-test-DB mistake, explicitly caught and re-run rather than reported as-is) → final clean run **4679 passed, 6 skipped, 0 failed, 0 errors** (`test_mcp_api_key_roles.py` deselected as confirmed pre-existing/environmental — hits a long-lived real backend's 10-API-key cap, unrelated to any column). Frontend: 1465/1466, the 1 failure a documented pre-existing flaky timeout. 5 real regressions found during the sweep, each individually root-caused (not just patched): a historical-migration test pinned to the wrong model-registry snapshot (fixed to resolve the graph's leaf state instead of a fixed old migration node — more robust, not just a workaround); 2 mock-chain test breaks from the new `select_related`/merged-query calls (fixed the mocks to match the new call shape); the `state_capture.py` merge above (redesigned from the brief's separate-query suggestion into a single merged query after a mock-collision failure revealed the better shape).
Also found: `_LIFECYCLE_MIRROR_MODELS`'s membership (2 types) and the column-drop scope (4 types) are different, compatible axes — resolved by testing both explicitly rather than assuming the brief's `FORMERLY_FLAGGED` test list matched the registry. Migration `0017`'s stated `RunPython.noop`-neutering condition (Step 3) verified NOT to apply (it doesn't import the removed helper; duplicates the mapping locally by design) — correctly left untouched rather than blindly edited per the brief's literal instruction.
Lighter-touch verification (usage-limit guidance, exceptionally strong self-verification already present — every discrepancy individually root-caused across 3 sweep iterations) — read migration `0075` in full (clean, pure schema drop, correct dependency) and the `state_capture.py` diff (correctly merges into the existing single Artifact header query instead of adding a second one, tenant-scoped, `"active"` fallback for workspace-less rows) myself; both confirmed correct.
Task 24: complete (commit 5c688afc). **Milestone M4 reached: one soft-delete flag, on `Artifact` only.**

## Task 25: The `ArtifactVersion` snapshot table

**Ruling: downgraded from the original senior-developer-reserved tier to standard (developer, sonnet).** The original coarse-grained tier list (Tasks 15/16/20/23/25/27/28 on senior-developer) was set before each task's actual brief existed, on the assumption Phase 5's tasks would carry the same architectural risk as Phase 3/4's data migrations. Task 25's brief, once generated, turned out to be purely additive schema (one new table, no existing data/call sites touched, an RLS migration that's an almost-verbatim copy of the already-established `0067_rls_remaining_pl_tables.py` pattern) with complete code supplied for every file — exactly the "plan text contains the complete code to write" case the SDD skill's model-selection guidance reserves for the cheapest capable tier. Cost if wrong: a straightforward reviewable diff either way; no data risk since the table is brand new. Task 26 (the service layer) and Task 27 (wiring writes) are where the real risk in Phase 5 actually lives — reassessing their tier when their briefs are generated rather than trusting the original list blindly.
Implementer: agent a99b66d295e7abbe6 (developer, sonnet). BASE=5c688afc.
Report DONE_WITH_CONCERNS: 5/5 new tests pass, full `persistence/` regression 244/244 (including `test_rls_coverage.py` confirming no new RLS gap). Migration round-trip explicitly verified forward→reverse→forward on a live test DB, not just static SQL comparison. Recurring `persistence.tenancy.set_tenant` landmine hit again (13th recurrence) — fixed to `TenantContext.set_tenant(...)`. Confirmed the tier-downgrade ruling was correct: this task was exactly as mechanical as predicted, no findings beyond the one recurring test-fixture bug.
Task 25: complete (commit babe18a7).

## Task 26: `ArtifactVersionService`

Standard tier (developer, sonnet) — mechanical, brief supplied complete service code. Implementer: agent a62108a360603fe39. BASE=babe18a7.
Report DONE: 7/7 new tests + full `application/tests/` regression 1679 passed, 0 failed. Both recurring landmines hit again (14th/15th recurrence of `set_tenant`/`AuthContext(roles=...)`), fixed. Verified `ServiceBase._set_tenant_context`, `Artifact.tenant`, and `ArtifactDiffService.list_versions_for_diagram`'s exact return shape against live code — no drift found, the brief's service code (Step 3) needed zero changes. Correctly verified the row-lock claim **by inspection** rather than trusting the "concurrent" test (which is actually sequential and cannot detect the lock's absence, per my explicit instruction) — confirmed `select_for_update()` on the parent `Artifact` row is the right lock target (no `ArtifactVersion` row exists yet on the first call, so locking the parent is the only option), and that a second concurrent `record()` would correctly block until the first commits.
Task 26: complete (commit f0b0b806).

## Task 27: Record a revision on every content write

**Senior-developer tier (per original ruling — highest-coverage task in Phase 5, 13 write paths).** Implementer: agent a14fb5fc0430b1ae1 (senior-developer, opus). BASE=f0b0b806.
Report DONE_WITH_CONCERNS: shipped 18 tests (not the brief's 10 — the brief's own list left 7 of 13 types untested, so added real end-to-end create+update coverage, through production service methods not mocks, for every type). Task 26 regression 7/7. Wide sweep `application/ diagram/ icd/ baseline/` (baseline/ added per the user's hard performance/baselining constraints, beyond the brief's own Step 7) → 2241 passed, 0 failed. Extra self-initiated sweeps: `rest_api/` 1266 passed, `workflow/ traceability/` 397 passed, full `rest_api/ mcp_server/ workflow/ traceability/ persistence/` 3094 passed with only the known pre-existing `test_mcp_api_key_roles.py` 10-key-cap red (confirmed unrelated). 9 documented deviations (D1-D9) from the brief's literal code, each individually verified against live source: `change_reason` genuinely absent from 9 of 18 call sites (bare copy-paste would have been 9 `NameError`s); update-recordings gated on each service's own existing "is this a real content change" predicate rather than the brief's unconditional snippet (avoids phantom-revision noise, mirrors the #269 finding this plan already fixed for the `version` counter); `MainGoalService` has no `create_version` method at all (real entry points are `create_manual`/`generate_from_goals`, both funneling through `_create_row` — recorded there, not in the retry-prone nested-atomic helper); `MainGoal` has no `lineage_id` column (workspace-scoped sequence instead — `lineage_anchor_artifact_id` extended to take an optional `workspace_id` rather than forcing a nonexistent field); Diagram/ICD deliberately reuse the existing `DiagramVersion`/`IcdVersion` version number instead of a fresh `MAX+1` (aligns the two numbering spaces for Tasks 28-29's retirement, removes a round trip, no collision risk since nothing else writes those types' `ArtifactVersion` rows yet); `ensure_artifact` called before the Ext-layer/glossary/CR recording sites to close a silent-skip branch for pre-Phase-3 rows that still have `artifact_id IS NULL`.
**Reviewed personally: the new `application/tests/conftest.py` autouse fixture** (affects the whole `application/tests/` suite) — narrowly scoped to short-circuit `ArtifactVersionService.record` only when the artifact_id isn't a real UUID (mocked-ORM test) or the tenant context is unarmed (`TenantContextNotSetError`), calls the real recorder otherwise, and `NotFoundError` still propagates (Task 26's own test still asserts on it). Confirmed this cannot mask a real recording bug in a DB-backed test — real end-to-end coverage lives entirely in `test_revision_recording.py`, which uses real UUIDs/real tenant context and so always executes the true recorder. Sound solution to a real problem (51 pre-existing mock-heavy tests carry `django_db` but mock the ORM away; editing each individually was the only alternative). Also spot-checked the `goal_service.py` lineage-anchor diff directly — matches the report exactly.
**Ruling on the reported findings (not fixed in this task, explicitly tracked rather than silently dropped):**
- `Adr.decision` missing from `_ENTITY_FIELDS` (mutable content, neither diffed nor snapshotted) — accepted as a Task 29 candidate, since fixing it changes `ArtifactDiffService` output, which is that task's remit, not this one's.
- **CSV bulk import (`application/import_service.py`) bypasses all 9 services and writes rows directly — imported artifacts get no revision 1.** This is a real, pre-existing architectural bypass (import never routed through the service layer, long before this task), not a regression this task's own change introduced (unlike Task 23's sibling-seam finding, which WAS caused by that task's own change) — ruled to accept as a documented, tracked gap rather than expand Task 27's scope into a file never in its brief. **Flagging to the user as worth a small dedicated fast-follow after Phase 5 lands**, since it means bulk-imported production data would permanently lack an original-import baseline in history.
- Decomposition-loop query cost (+3 to +6 queries/node in `architecture_decompose_service.py`/`requirement_service.py::decompose`) — accepted as measurably present but not order-of-changing (the pre-existing embedding-generation call in the same loops already dominates cost); no speculative bulk-recording API built for a single caller without a profile justifying it.
Task 27: complete (commit ca1d5ecb).

## Task 28: Migrate and retire the three legacy version tables

**Senior-developer tier (per original ruling).** Implementer: agent ab699463637b825a0 (senior-developer, opus). BASE=ca1d5ecb.

**Major plan-defect finding, NEEDS_CONTEXT — escalated to the user rather than ruled on myself, unlike Task 23.** Unlike Task 23's finding (a code-level contract-preservation fix with one clear right answer), this finding has real product-capability consequences (dropping a shipped ICD semantic-search feature) and substantial, genuinely open-ended engineering cost tradeoffs — squarely a "destructive operation on live data / product decision" the SDD skill's hard-stop criteria reserve for the human, not an implementer or coordinator ruling.
The brief (and ultimately spec §6.2 / Decision D-4) assumed all three legacy version tables (`DiagramVersion`, `IcdVersion`, `GlossaryTermVersion`) are pure history tables. Verified false for two of three: `Diagram` has **zero** content columns (payload/canvas live only on `DiagramVersion`) and `Icd` has **zero** contract columns (direction/interface_type/preconditions/postconditions/invariants + a pgvector embedding with HNSW index live only on `IcdVersion`). Only `GlossaryTermVersion` is genuinely history-only (`GlossaryTerm` already carries all current content). Literally dropping all three per the brief would delete the live content of two subsystems and a shipped feature (`IcdVersion.embedding` backs ICD semantic search via `application/search_service.py`'s `_EMBEDDABLE_TYPES`; `ArtifactVersion` has no vector column). Blast radius reported: 32 production modules / ~250 references (heaviest: `icd/icd_manager.py` 38, `diagram/manager.py` 23, `icd/services.py` 20).
**What WAS implemented and is safe to land now regardless of the ruling:** `persistence/migrations/0078_migrate_legacy_versions.py` — copies all 146 real legacy version rows (verified against a `pg_dump` clone of the actual accumulated dev DB, not just a fixture) into `ArtifactVersion`, non-destructively (drops nothing). Found and closed two guard gaps the brief didn't have: (1) all three source tables + their owners + the `ArtifactVersion` target carry FORCE ROW LEVEL SECURITY — added the now-standard `SET LOCAL row_security = off` guard (same precedent as `0073`/`0074`/`workflow/0018`), since without it the migration would have silently "succeeded" copying zero rows; (2) Task 27's write path already mirrors new writes into `ArtifactVersion` at the same `(artifact, revision)` coordinates the copy targets (Diagram/Icd reuse `version_number`, GlossaryTerm auto-increments from a *different* base than `term_version` — these do NOT share a base, so a naive copy could silently corrupt history on any DB that served traffic before migrating) — added an identical-payload-is-idempotent / differing-payload-fails guard, strictly stronger than the brief's. Rehearsal against the real dev-DB clone: guard correctly fired on 2 genuinely workspace-less legacy `GlossaryTerm` rows (known E2E/MCP test debris, same class Task 20 already documented as unbackable), then a clean 146/146 copy with original timestamps preserved (not collapsed onto the migration date), then a third re-run proved full idempotency at real scale (146 stayed 146, zero duplication).
**Ruling on the smaller, lower-stakes open question (workspace-less GlossaryTerm rows blocking a real `0078` apply):** accepted the implementer's own recommendation — ship as-is with a runbook step (`manage.py check_artifact_backing` as a pre-deploy gate, which already reports these rows) rather than inventing a data migration that assigns an arbitrary workspace to what is almost certainly test debris on the operator's behalf. Low-risk, reversible (a runbook line, not a schema change) — ruled directly rather than escalating.
**User's ruling on the big question (asked via AskUserQuestion, 2026-09-06): full Expand/Contract migration (28c), not a scoped-down §6.2.** Plus two follow-up decisions: **(i) ICD embedding moves to a new vector column directly on `Icd`** (with its own HNSW index), replacing `IcdVersion.embedding` — ICD semantic search is preserved, now indexed on the current ICD state rather than per-revision. **(ii) `IcdParameter` is treated exactly like every other artifact's fields** — repointed directly at `Icd` (no more per-revision FK to `IcdVersion`), with historical parameter states reconstructable exclusively through `ArtifactVersion` payload snapshots (via `_ENTITY_FIELDS["Icd"]`/`snapshot_fields`) exactly like every other type, not through a separate per-revision row-linkage mechanism.
Task 28 split, per the implementer's own recommendation:
- **28a (this section, commit PENDING):** land `0078` now — safe, non-destructive, unblocks nothing, blocks nothing.
- **28b (next):** retire `GlossaryTermVersion` only — clean, small, standard-tier (same shape as Task 24's schema drop).
- **28c (after 28b):** the Expand/Contract for Diagram and ICD — move current content onto the header rows, backfill from `current_version`, add the ICD embedding column + HNSW index, repoint `IcdParameter` at `Icd`, migrate the ~32 affected modules, then drop `DiagramVersion`/`IcdVersion` and swap `current_version` → `current_revision`. Senior-developer/opus tier — this is now the largest single-task blast radius in the whole plan.
Task 28a: complete (commit 1d5fd5b8).

## Task 28b: Retire `GlossaryTermVersion`

Standard tier (developer, sonnet) — self-authored dispatch (not from the plan's generated brief, since this is a coordinator-initiated split of the original Task 28). Implementer: agent a362fbff5e970c1b4. BASE=1d5fd5b8.
Report DONE: full `persistence/ application/ rest_api/` regression 3199 passed, 1 skipped (pre-existing, unrelated), 0 failed. Removed the dual-write in `GlossaryService.create()`/`.update()`, deleted the model, dropped the table via migration `0079` (correctly depends on `0078`, confirmed explicitly). Reader sweep found one thing the dispatch didn't anticipate (`GlossaryTermVersionSerializer` was dead code, never wired to any view — removed outright) and did a real reimplementation (not just compile-fix) of `ArtifactDiffService`'s glossary version/diff helpers through `ArtifactVersionService`, since the existing return shapes already matched exactly what the old hand-built code produced — verified this personally by reading the diff, clean 1:1 replacement.
**Real, well-reasoned deviation found in Task 28a's own test file** (`test_legacy_version_migration.py`): its `_migrate_history()` helper uses the *live* `django.apps.apps` registry (a deliberate Task 28a shortcut, not a bug) rather than a real historical migration-state registry, so once `GlossaryTermVersion` left `models.py` entirely, every test in that file — not just glossary-specific ones — would have hit `LookupError` via the `SOURCES` list's unconditional 3-model resolution. Fixed scoped to the test file only (a temporary monkeypatch of `SOURCES` for the duration of each call, restoring after) — the already-applied `0078` migration itself is untouched and unaffected in real `manage.py migrate` runs, which use a proper historical registry. One glossary-specific fixture test deleted as having no meaningful post-drop equivalent (the behavior it verified was already proven once, while the table existed, in Task 28a's own commit).
Task 28b: complete (commit 72417691).

## Task 28c: Expand/Contract for Diagram and ICD (user-ratified scope, senior-developer tier)

Split further by the coordinator into two safe stages, since this is now the largest single-task blast radius in the plan (32 modules / ~250 refs per Task 28's investigation):
- **28c-1 (dispatched):** Expand only — additive schema (content columns on `Diagram`/`Icd`, `current_revision` counters, new `Icd.embedding` vector column + HNSW index, new `IcdParameter.icd` FK alongside the existing `icd_version` FK) + a verified, RLS-guarded backfill from `current_version`. Zero reader changes, zero table drops, zero removed fields — the whole point is that every existing test must still pass unchanged, proving Expand introduced no behavior change before the risky Migrate/Contract stage touches anything.
- **28c-2 (after 28c-1 lands):** migrate the ~32 reader modules (write paths in `icd/icd_manager.py`/`diagram/manager.py` switch to writing the header columns directly instead of creating version rows; `application/artifact_diff_service.py`'s Diagram/Icd version/diff helpers rewritten against `ArtifactVersionService`, same pattern Task 28b just established for GlossaryTerm; `application/search_service.py`'s `_EMBEDDABLE_TYPES` repointed at `Icd.embedding`; etc.), rehearse against real dev-DB-clone data, then the actual Contract (drop `DiagramVersion`/`IcdVersion`, remove `current_version`/`icd_version` FKs).
Implementer (28c-1): agent a62bd1f294b5ffba0 (senior-developer, opus). BASE=72417691.
Report DONE: purely additive, zero regressions proven — 20 new tests (including 4 that prove the `RuntimeError` guards genuinely fire and 4 that prove the RLS guard actually executes via `CaptureQueriesContext`, a first for this codebase's migration tests) + full 6-directory sweep 4855 passed, reconciling exactly with 28's 4833 + 28b's +2 + this task's +20 → zero regressions. Rehearsed against the real dev-DB clone: 20/20 Diagrams and 96/96 ICDs backfilled with zero mismatches, the one real ICD embedding vector carried over intact (verified via independent `IS DISTINCT FROM` SQL, not the migration's own count).
**Two design decisions, both reasoned from live-code verification rather than guessed, both accepted:**
1. **`IcdParameter` snapshot bridge:** added `Icd.parameters_snapshot` (a read-only property rendering the current parameter set as a JSON-safe by-value list — reads via `unscoped` with an explicit tenant_id since the related manager would raise `TenantContextNotSetError` on the Ext-layer write path; stringifies `Decimal` bounds since `ArtifactVersion.payload`'s plain `JSONField` has no custom encoder and `json.dumps` rejects `Decimal`; deterministic `ordering`-then-`name` sort so an unchanged parameter set never diffs as "changed"). Deliberately NOT wired into `_ENTITY_FIELDS["Icd"]` yet — that's for 28c-2, once the actual writer also emits it, so "behavior provably unchanged" holds for this Expand-only step.
2. **Historical `IcdParameter` backfill — flatten to per-ICD, not per-revision, backfilling every row regardless of source version.** Verified this is not actually a tradeoff: `IcdParameterService.update_parameter/.delete_parameter` already mutate parameter rows in place (only `IcdVersion` itself is under the immutability trigger, per that service's own docstring), and `IcdManager.update_icd` has zero occurrences of "parameter" in its body — it never carries parameters forward to a new version. So "a parameter set frozen with its revision" is not a semantics this system actually maintains today; backfilling only the current version's parameters would have deleted live, user-visible data reachable via a real GET endpoint. Real dev data has zero `icd_parameter` rows and every ICD has exactly one version, so this path had no real-data coverage — the implementer injected synthetic rows into the rehearsal clone rather than let it pass vacuously, a good instinct.
Carries forward a detailed, concrete 14-item punch list for 28c-2 (writers before readers, the exact `_ENTITY_FIELDS`/snapshot wiring order including a documented one-time "parameters_snapshot renders as added on every pre-existing ICD's first post-cutover diff" honest side-effect, the ranked ~32-module reader list, and the Contract-phase drop order) — genuinely useful, reduces 28c-2's own investigation burden.
**Carried-forward concern, not this task's to fix:** the same two workspace-less legacy GlossaryTerm rows from Task 28's open question are now on the critical path for two more migrations (`icd/0010` depends transitively on `persistence/0079`/`0078`) — unchanged recommendation (ship with a `check_artifact_backing` pre-deploy gate), now doubly relevant.
Task 28c-1: complete (commit 5024bcc6).

## Task 28c-2: Migrate writers/readers + Contract (drop DiagramVersion/IcdVersion)

Implementer: agent ad05fa980a75b5667 (senior-developer, opus). BASE=5024bcc6. Dispatched with 28c-1's own concrete 14-item punch list (report §8) as the starting spec, explicit permission to split further into a 28c-3 if the full Migrate+Contract scope proves too large to verify safely in one dispatch (same pattern as the original Task 28's own 28a/28b/28c split), and explicit open decisions carried over for it to resolve or ask about: the `?version=` API-shape question on IcdParameter's flattening, dual-write-vs-cutover for the writers, the duplicate-parameter-name question, and confirming the `IcdVersion` immutability trigger doesn't obstruct the eventual `DROP TABLE`.
**Report DONE after ~3h20m, the largest single task this whole plan (290 tool uses, 571K tokens).** All four parts (writers, parameter-snapshot wiring, readers, re-backfill+Contract) landed in one change — judged not to need a further 28c-3 split, since `IcdParameter.icd`'s NOT NULL requirement makes a schema migration unavoidable even for a partial landing, removing most of the value of splitting further. Both `DiagramVersion` and `IcdVersion` are now dropped.
**Re-counted the "~32 modules" claim rather than trusting it: real production surface was 21 modules** (28c-1's estimate included test files and one false positive — `architecture_service.py`'s own unrelated `current_version` optimistic-lock variable). Introduced two by-value read models (`DiagramRevision`/`IcdRevision`, frozen dataclasses rehydrated from an `ArtifactVersion` payload with the same attribute names the retired ORM rows exposed) so most call sites needed zero changes to their attribute access, deliberately not models since history is no longer a table.
**All four of 28c-1's flagged open decisions resolved, each verified rather than assumed:**
1. **Full cutover, not dual-write** — `IcdParameter.icd_version` being `NOT NULL` makes a schema change unavoidable either way, so a transitional dual-write would only buy a second permanently-untested code path.
2. **`?version=` on ICD parameters serves from the `parameters_snapshot`** (not 400, not silently ignored) — the only option that never answers a question about revision N with revision-current's data. Added `IcdRevision.parameters_captured: bool` so "no parameters recorded" and "parameters never captured" (pre-cutover revisions) are distinguishable — a missing flag would have made the API return a confident, wrong `[]` for every historical revision recorded before this change.
3. **Duplicate parameter names after flattening: no de-duplication**, checked against real data first (0 rows, so injected the case into the rehearsal clone to actually exercise it) — reasoned that the flattening introduces no new invalid state (no unique constraint existed on `(icd_version, name)` either), there's no correct automatic winner between two differing historical rows, and dropping rows in a migration is never the right default.
4. **Immutability trigger vs `DROP TABLE`: confirmed empirically** (both prior reports had flagged this as unverified) against a real-data clone — a trigger cannot outlive its dropped table, so `DROP TABLE` handles it automatically; the standalone `icd_raise_version_immutable()` function it left behind as an orphan is explicitly dropped with a `reverse_sql` that restores it, since a leftover plpgsql function is invisible to any ORM-level check.
**Self-caught bugs during self-review** (not found by tests, found by re-reading the own diff): an `int(request.data.get("version"))` sitting outside its `try` block (would have 500'd on a non-numeric value); two more unhandled `ValueError` paths on the GET branch; a duplicate-kwarg `SyntaxError` from the implementer's own bulk edit.
**Personally verified: read `icd/migrations/0013_drop_icd_version.py` (the hand-written one — flagged by the implementer itself as deserving "a second reader's eye" since `AlterField` NULL→NOT NULL can't be auto-generated) and `icd/migrations/0012_final_backfill_before_contract.py` in full.** Both are textbook-correct against every established pattern in this plan: RLS guard, null-safe `IS DISTINCT FROM` verification, `atomic=True` load-bearing twice, correct operation ordering (parameters re-owned and verified non-null BEFORE the FK is flipped to NOT NULL and the old FK dropped), correct dependency chain back to `persistence/0078` so no path through the graph can drop history before it's copied.
**Rehearsal methodology upgraded again**: staged specifically to reproduce the actual deploy shape rather than migrating straight to head (which would have made the re-backfill trivially vacuous) — migrated the real-dev-DB clone to exactly the 28c-1 state, injected drift (writes that only touched the legacy version tables, simulating what pre-cutover code would have produced in the gap between 28c-1's backfill and this task's cutover), THEN migrated to head, and independently verified the drift was repaired on both sides (`icd_drift_repaired: 1`, `diagram_drift_repaired: 1`) — proving the final re-backfill actually does its job, not just that the happy path works. Followed by an end-to-end smoke test of real write/read paths against the migrated real data with the real embedding provider active.
Full 8-directory regression: 5268 passed, only the known pre-existing `test_mcp_api_key_roles.py` red (unrelated, same baseline documented since Task 28). Frontend: 16 passed, `tsc --noEmit` clean of anything related to the breaking wire changes (`current_version`→`current_revision`, `icd_version_id`→`icd_id`, `version_id` removed from similar-ICD hits) — each grepped through `frontend/src`/`e2e/` and confirmed genuinely unread before changing.
**Carried-forward concerns, none blocking:** the same two workspace-less GlossaryTerm rows are now on the critical path for 4 migrations total (runbook gate still the right, still-unwired answer); workspace-less Diagrams lose their version history as a direct, bounded, irreversible-once-`0012`-runs consequence of the full-retirement ratification (no production path creates them today, one dead code path threaded with an optional `workspace_id` anyway as a 4-line hardening); `persistence/tests/test_legacy_version_migration.py` deleted since all 3 of `0078`'s source models are now gone from the live registry (its guarantee now rests on the realistic-data rehearsal, exercised 3 times across this plan's arc with real production-shaped data — arguably stronger coverage, just not in the CI suite anymore).
Task 28c-2: complete (commit ce58b486). **This is the largest single task in the entire plan, and the second Milestone-M5-adjacent architectural correction this session's Task 28 investigation produced beyond the original plan's literal scope.**

## Task 29: One diff world — Milestone M5 (FINAL TASK OF THE PLAN)

Standard tier (developer, sonnet) — pure code consolidation, no data migration risk. Implementer: agent ae2e44099d651acfd. BASE=ce58b486.
Dispatched with an explicit heads-up that the brief was almost certainly written before Task 28c-2 landed: 28c-2's own report flags that it already rewrote (not deleted) the Diagram diff/version helpers to stop referencing the dropped table, Task 28b already did the same for GlossaryTerm, and — most importantly — 28c-2 explicitly deferred ICD's REST diff/versions unification to this task ("Unifying them is Task 29's job") with no mention in the brief at all. Instructed to investigate and resolve the ICD question rather than let M5's "one diff world" claim be silently untrue for the one type that most needed unification.
**First attempt (agent ae2e44099d651acfd) stalled silently after ~3h of otherwise-real progress** (14 files genuinely modified, ICD-unification test scaffolding started). Detected via a deliberate freshness check (no `backend-test` Docker container running or recently exited; no file on disk touched in 2h+) rather than trusting `ListAgents`' own `status: running`, which does not detect this kind of stall. A `SendMessage` status check-in was accepted ("queued for delivery at its next tool round") but produced no reply or further activity — confirmed genuinely wedged, not just slow. `TaskStop`'d and filed as product feedback (background `local_agent` task showing `running` indefinitely with zero observable progress, unresponsive to a check-in). **Resumed fresh** (agent ac3ab7aace1d0d14a) with the stalled attempt's uncommitted file changes left in place as a starting point, explicit instructions to verify/judge each one rather than trust it, and permission to revert and redo from scratch if the prior state is too broken to build on safely.
**Process lesson for the rest of this session and future SDD executions:** for any implementer dispatch running past ~90 minutes, check file-mtime staleness + Docker container activity (not just agent status) as a concrete liveness signal before assuming a long task is merely thorough — this plan has legitimately had multi-hour tasks (Task 27 ~5.5h, Task 28c-2 ~3h20m) so duration alone is not a stall signal, but zero observable output for 2+ hours combined with no active verification process is. Filed as `agent-meta` issue [#681](https://github.com/Popoboxxo/agent-meta/issues/681) for the framework's own SDD skill guidance.
**Fresh resumption (agent ac3ab7aace1d0d14a) vindicated the "hand over the stalled work, verify don't trust" approach**: read every one of the 12 inherited files against live code and found them **substantially correct and complete, nothing to revert** — the stalled attempt had genuinely finished the hard part (including the ICD-unification decision, correctly resolved as "unify," and a real latent gap it closed: the hand-rolled REST diff only compared 6 fields, the unified path correctly compares all 8 via `_ENTITY_FIELDS["Icd"]` including `parameters_snapshot`) before it stopped producing output. Only one genuine gap existed, in a file the stalled attempt never touched: `test_artifact_versioning_audit.py`'s Diagram/GlossaryTerm assertions still expected the old type-specific "every entry always has content" shape, which is no longer true now that `list_versions()` uniformly prepends a `content_available: False` v0 baseline for every type — correctly diagnosed as the intended, correct consequence of "one versioning mechanism for all types," not a bug, and fixed to match the pattern the pre-existing ICD test already used.
**Personally spot-checked the ICD unification diff in `icd_views.py`** — clean, minimal, matches the report exactly.
Final verification: gate test 14/14, fixed regression file + gate 29/29, wide 9-directory regression 5384 passed / 6 skipped / 0 failed (only the known pre-existing `test_mcp_api_key_roles.py` API-key-cap flake deselected, same baseline documented since Task 28), migration-drift check clean (correctly touches no models).
Task 29: complete (commit PENDING). **MILESTONE M5 REACHED: one diff world for every artifact type, including ICD.**

## PLAN COMPLETE — all 29 tasks (plus the 15b/28a/28b/28c-1/28c-2 splits) done, M1 through M5 all reached.

Per explicit user instruction (2026-09-06): skipping the SDD skill's normal final whole-branch review — notifying the user directly instead once this last commit lands. Two major, well-reasoned architectural corrections this session's own investigation produced beyond the original plan's literal text: Task 23's soft-delete wire-contract reconciliation (Options ratified by the coordinator) and Task 28's discovery that `DiagramVersion`/`IcdVersion` were not pure history tables, requiring the user-ratified 28a/28b/28c-1/28c-2 Expand/Migrate/Contract split (the largest body of work in the whole plan). Per the standing 11-plan-sequence instruction, the next plan (2. Attribut-Definition, already spec-confirmed via D3/D6 on 2026-09-06) starts next without further prompting, once this commit is confirmed.

---
